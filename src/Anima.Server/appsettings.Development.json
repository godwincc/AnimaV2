{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
  // ConnectionStrings:Default is intentionally not set here -- run
  // `dotnet user-secrets set "ConnectionStrings:Default" "Host=localhost;Port=5432;Database=anima_dev;Username=anima;Password=anima_dev_password"`
  // once locally instead (see README-dev-secrets.md). Development env auto-loads user-secrets.
}
