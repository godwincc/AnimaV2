# Anima (AnimaV2) — Project Context for Claude Code

This file is auto-read by Claude Code at the start of every session.

## What This Project Is

A breeding roguelike deckbuilder ("Anima") inspired by Axie Infinity (breeding/parts) and Slay the Spire (roguelike run structure). Players breed creatures called **Anima** — framed as constructs/"Vessels," not literal animals — to obtain skill combinations, build a 3-Anima team, and run a node-based roguelike dungeon.

**Core design pillar:** reward SMART breeding, smart team composition, smart deckbuilding — not just raw stats.

**Current phase:** The entire backend loop is real and tested end-to-end (140/140 checks passing as of this session). Full art direction is established. All 6 core screens are fully locked (Hub, Weaving, Sanctum, Anima Profile, Delve, Collection). All 4 room-encounter screens are locked (Resource, Treasure, Shop). **The Combat screen is fully designed.** **The match result screen (Victory/Defeat/Retreat) and the new Anima Reveal screen are now also fully designed and backed by real implemented services this session** — see Match Result & Retreat System and Anima Reveal Screen below. **Godot client implementation is underway** — a real `client/` Godot 4.7.1 project has a proven `GameHub` connectivity pipe and a real first UI screen (Login/Create Account, see the Login / Create Account Screen section below); no real Hub/Sanctum/Weaving/Delve/Combat screens exist yet. (This paragraph is otherwise a stale snapshot from an earlier point in the project — e.g. it undercounts the current console-harness check total and doesn't mention the Server/Accounts/Auth or Delve-related work documented later in this file; flagged here rather than fully rewritten, since only the Godot-client clause was in scope for this pass.)

**Real pixel-art creature portraits are deferred** to a future outsourced pixel artist — Claude has no native raster/pixel image generation (architectural limitation of language models, not a subscription tier issue). Retrofitting real art into Godot later is confirmed easy: sprites are just texture references, so swapping a placeholder for a real PNG requires no structural changes.

## Tech Stack & Architecture

C#/.NET 10 backend, fully built and tested. Planned client: Godot (GDScript). Server: ASP.NET Core/SignalR, first-pass implemented this session — see Server / Accounts / Auth below.

**IMPORTANT:** there is NO real `CombatEventBus`. Every "event" is a direct inline check at a known checkpoint.

**Recurring gotcha:** root namespace "Anima" collides with the `Anima` class (CS0118). Prefer `using AnimaUnit = Anima.Core.Models.Anima;` as the default fix everywhere — more reliable than moving `using` above the namespace.

**Recurring gotcha:** `DelveRun.Team` and a Combat node's `CombatState.PlayerTeam` must be the **same `List<Anima>` reference**, never a rebuilt/cloned list — HP attrition across nodes works today only because `CurrentHp` mutations happen in place and propagate for free through shared object references. Any future Combat-node wiring (Godot client, Server project) that constructs `CombatState.PlayerTeam` from a copy of `DelveRun.Team` will silently break attrition. Same caution applies to `CurrentArtifacts`/`WispEarnedSoFar` on `DelveRun`, which are live passthroughs to `RunLedger.Artifacts`/`PersistentLedger` rather than separate copies — do not fork these into independent state that must be kept in sync by hand.

### Solution structure
```
AnimaV2.sln
src/
  Anima.Core/  <- PERMANENT, fully built and tested (Models/Combat/Map/Reforge/Weaving/Economy/Data)
  Anima.ConsoleHarness/  <- THROWAWAY test runner + full Delve simulation (de facto test suite; tests/Anima.Core.Tests does not exist yet)
  Anima.Server/  <- first-pass implemented this session (Auth/JWT/SignalR GameHub/DB persistence), see Server / Accounts / Auth below
tests/
  Anima.Server.Tests/  <- default `dotnet new` template stub only, no real tests written yet
```

## Map Generation (LOCKED, VALIDATED — 500 seeded maps pass every rule)
7×15 grid + Boss. Fixed floors: 1=Combat, 9=Treasure, 15=Shop. **Random odds, Floors 6+ (REBALANCED THIS SESSION — Treasure cut): Combat 37%/Elite 16%/Resource 16%/Treasure 7%/Shop 18%/Reforge 6%.** Previous session's split was Combat 34%/Elite 15%/Resource 15%/Treasure 15%/Shop 16%/Reforge 5%. Treasure's prior 15% (~14.21/map real-measured) was a confirmed real balance gap, not a perception issue: Artifacts are hard-capped at 3 held/Delve with no swap mechanic, so the large majority of Treasure nodes at that volume were structurally guaranteed no-ops (already-at-cap, or a wasted duplicate roll) — confirmed against STS's own Treasure Room rate for comparison (STS guarantees only ~1 per 17-floor act, roughly 3-5 relics across a full 3-act run). Treasure cut 15%→7%; the freed 8 points redistributed as Combat +3/Elite +1/Resource +1/Shop +2/Reforge +1 (a deliberate judgment call favoring Combat/Shop over a flat split, not an even +1.6 each). **The old "Reforge must stay last in the odds array" invariant is GONE, not just unstated** (unchanged from the prior session's own finding): it only ever existed to guarantee a 0.0-weight entry stayed truly unreachable; now that no entry is 0-weight, every entry has a real span in the weighted-roll loop, so array order no longer affects reachability at all.

**Guaranteed Elite + Early-Game Elite Exclusion (LOCKED, STS-inspired, untouched by this session's odds rebalance):**
1. **Floors 1-5: Elite excluded entirely.** Uses a separate `EarlyFloorTypeOdds` table (**REBALANCED THIS SESSION, same Treasure-cut reasoning as the Floor-6+ table above: Combat 45%/Resource 30%/Treasure 8%/Shop 17%**, Elite/Reforge omitted, not zero-weighted) instead of the Floor-6+ table. Previous session's split was Combat 40%/Resource 20%/Treasure 20%/Shop 20%. Treasure cut 20%→8%; the freed 12 points redistributed as Combat +5/Resource +10/Shop **-3** — Shop deliberately did NOT get a share this time (its own weight here never manifests on a real map regardless, see below), so its -3 was folded into Resource's +10 instead of sitting on an already-inert number. **Shop's entry is intentionally left inert (LOCKED, confirmed deliberate — not a bug):** Shop is *also* independently banned on Floors 1-5 by the pre-existing `EliteShopMinFloorIndex` rule (a joint Elite+Shop floor gate from an earlier session, untouched by this one), so that weight never actually manifests on a real map — the real, observable Floors-1-5 distribution is Combat/Resource/Treasure only, renormalized among just those three. Reforge is likewise omitted here, structurally, independent of whatever weight it holds in the main `TypeOdds` table — Floors 1-5 simply never offer Reforge at all, same as Elite.
2. **Floors 6-14: guaranteed ≥1 Elite.** Before the normal weighted roll runs anywhere, one random eligible node is force-assigned Elite directly (bypassing the odds table entirely). This is a floor, not a cap — the normal roll still includes Elite at its own Floor-6+ weight for every other node afterward, so a map can have more than 1 Elite, never fewer. **Flagged deviation from the literal brief (unchanged from a prior session):** eligibility is Floor 6-13, *not* Floor 6-14 as specified — Floor 14 had to be excluded because every Floor 14 node connects directly into Floor 15 (entirely fixed Shop), and Elite-Shop direct adjacency is already banned by the existing `NoAdjacentGroup` rule. Re-validated this session (asked for explicitly): the guarantee is completely untouched by the Treasure odds rebalance — the forced assignment is a direct `.Type =` write on one node, never touching `TypeOdds`, `EarlyFloorTypeOdds`, `allowed`, or the weighted-roll loop, so every map still gets ≥1 Elite regardless of any table's weights.

Real re-validated data at the final odds (n=500 seeds, permanent batch validator built into the harness — 0 rule violations, 0 Elite occurrences on Floors 1-5, every map has ≥1 Elite, all reconfirmed not assumed): **avg/map — Combat 24.53, Elite 4.89, Resource 12.87, Treasure 8.89, Shop 8.09, Reforge 1.20, Boss 1.00.** Previous session's real-measured avg/map: Combat 22.73, Elite 4.53, Resource 10.31, Treasure 14.21, Shop 7.67, Reforge 1.02, Boss 1.00 — Treasure dropped ~37% (14.21→8.89) for the ~53% weight cut (15%→7% main table), a smaller-than-linear drop specifically because Floor 9 is UNCONDITIONALLY forced to Treasure for every column regardless of any `TypeOdds` weight (`node.Type = MapNodeType.Treasure` for the whole floor, confirmed by re-reading `MapGenerator` directly, not assumed) — that fixed floor contributes a large, weight-independent baseline that the random-roll weight change can never reduce below. **Flagged, not silently smoothed over: this real 8.89/map lands above the "5-7/map" target ballpark the rebalance was aimed at** — hitting that range purely via `TypeOdds`/`EarlyFloorTypeOdds` tuning is structurally impossible without also changing Floor 9's fixed-Treasure rule (out of scope for this pass, which only touched odds weights per what was actually asked). Still a real, substantial improvement over the prior 14.21 — just not landing inside the originally-hoped-for range, and worth knowing before further tuning is attempted.

**Node types (6):** Combat/Elite/Resource(Wisp-only, deliberate)/Treasure/Shop/Reforge/Boss. Elite optional/skippable (still true from Floor 6 on — the guarantee is a minimum count, not a forced route through it).

## Reforge (LOCKED — color-first flow, ported to GameHub)
Flow: pick color ("Aspect" in UI) → browse all 9 Head/Frame/Tail skills for that color (Crest excluded, no deck cards) → pick target Anima (no-op targets disabled) → confirm Accept/Decline. Accept commits a `DelveRun`-scoped override (`SetReforgeOverride`/`GetEffectiveSkill`), never mutating the Anima's real Head/Frame/Tail — consulted at deck-build time, survives across multiple combats in the same Delve. Cost: 40 Wisp same-color, 80 cross-color or hybrid. `ReforgeService.Accept` returns a distinct `InsufficientWisp` outcome (not an exception) — nothing commits, node stays open to retry. "Aspect" in the UI means color only (the old Head/Frame/Tail meaning is fully retired). Visual identity: standard warm gradient, hammer icon accent. Ported to `GameHub`: `GetReforgeBrowseOptions`/`GetReforgeValidTargets`/`AcceptReforge`/`DeclineReforge` — only a successful Accept or explicit Decline clears the node. (full history: docs/history.md)

**Godot client (NEW this session): `reforge_node.tscn`/`reforge_node.gd`, following the exact same child-panel/shared-`HubConnection` architecture Resource/Treasure already established** (instanced into `delve.tscn`'s `NodeEncounterOverlay` by `delve.gd`'s `_handle_node_arrival`, not a top-level scene — see resource_node.gd's own top-of-file comment for why a real scene change would orphan the in-progress `DelveRun`). Unlike Resource/Treasure, nothing resolves on `start()` — Reforge is browse-then-commit, so the first real RPC (`GetReforgeBrowseOptions`) only fires once the player picks an Aspect; there is no parameterless "list Aspects" endpoint, since the 4 Aspects are a fixed client-known set (Crimson/Onyx/Verdant/Azure, confirmed by reading `ReforgePartPool`, which is built purely from `PrimitiveRoster`'s 4-color archetypes — no hybrids). One panel, four steps (Aspect select → skill browse → target picker → Accept/Decline confirm) swapped via visibility, not sub-scenes; a Back button navigates within the sequence, a persistent Decline link/button exits from any step. `signal resolved` fires only on a successful Accept or an explicit Decline, matching the locked "only Accept/Decline clears the node" rule — backing out via Back never emits it. Cost and "already equipped" exclusion are both read directly off the server's `ReforgeValidTargetsResult` (per-target `Cost`, already Ember Core-discounted; `InvalidTargetAnimaIds`), not recomputed client-side. `IconGlyph` already had a `"hammer"` icon kind from the Delve map screen's own node-icon set, reused here for the header accent and the larger centered Aspect-select landing icon — no new icon shape needed. `delve.gd` gained a third DEV-only force-node button (`DevForceReforgeButton`, same BFS-to-nearest-matching-node-type mechanism the Resource/Treasure buttons already use) since Reforge is a real but low-probability (6% Floor 6+, 0% Floor 1-5) node type.

**Verification gap, flagged not silently skipped:** the new screen passed a headless scene-instantiation check (loads cleanly, `resolved` signal and `start()` method present, no `@onready` path errors) and its server-facing calls were traced directly against `GameHub.cs`/`GameHubModels.cs`/`ReforgeService.cs`'s real signatures, not assumed. A real Web-build click-through screenshot pass was attempted (~35 login/register attempts across 6 export cycles) but blocked entirely by TODO #18's `HTTPRequest` failure, which reproduced 100% of the time this session — see TODO #18 for the new diagnostic detail (a real `RESULT_TIMEOUT`, not just a vague "could not reach the server"). Live verification of the Reforge screen's actual click-through behavior is still outstanding.

**Real bug found and fixed, follow-up session: `delve.gd`'s `_team_list` was typed `VBoxContainer`, but `delve.tscn`'s real `TeamList` node is an `HBoxContainer`** (confirmed correct per delve.gd's own `_build_team_card` comment — "TeamList is now an HBoxContainer of 3 of these side-by-side" — and matches Hub's own `TeamRow`, also `HBoxContainer`; the scene was right, the type annotation was wrong). Confirmed via `git stash` that this predates the Reforge work entirely (introduced by the prior "Delve layout fixes and Artifact/Team restructure" commit, which changed `TeamList`'s role from "a VBoxContainer of horizontal rows" to "an HBoxContainer of vertical cards" without updating the onready var's type). **Impact was NOT limited to the Team panel, confirmed by reading the actual error cascade, not assumed:** GDScript's typed `@onready` assignment throws and aborts the rest of `implicit_ready()` on a mismatch, so every `@onready var` declared AFTER `_team_list` (resource/artifact lists, map-confirm controls, info label, retreat overlay, end-summary controls — 13+ variables) also silently stayed null. `_ready()` then threw a second time trying to connect a signal on one of those null buttons, meaning `_run()` — the method that actually opens the hub connection, calls `StartDelve`, and renders the map/team/resources/artifacts — never executed at all. **The real, pre-existing bug was a completely non-functional Delve screen, not a cosmetically-broken Team panel.** Fixed by retyping `_team_list` to `HBoxContainer` (one line, `delve.gd`) rather than touching the scene. Re-verified headlessly: scene now loads with zero script errors.

**Live click-through re-verification attempted, blocked by a NEW, broader networking finding (informational only — not investigated further, out of scope for this pass):** got as far as a real Hub screen render for one fresh account via a temporary test-only auth bypass (real JWT obtained via curl, injected directly into `AuthState`, since login itself is blocked by TODO #18) — confirmed real SignalR connectivity and real Hub content that way. But the SAME technique against a second account (needed for a real 3-Anima team to check the fixed Team panel and exercise Reforge) hung indefinitely on "Connecting to GameHub…", and re-testing the ORIGINAL working account afterward hung identically — ruling out anything specific to one account/token. A raw Node.js WebSocket test (same JSON Hub Protocol technique this codebase's own history already uses) confirmed the SERVER side answers `GetPendingStarterReveal` instantly and correctly for the second account (real 3-Anima starter trio, real data) — so the hang is entirely inside Godot's own Web-exported networking bridge, not the server, not CORS, not this fix. This suggests TODO #18's degradation may not be limited to `HTTPRequest`/REST calls as previously scoped — SignalR/WebSocket connections hung too, in the same browser session, later in the same pass. Flagging this as new information for whoever eventually investigates TODO #18; deliberately not chased further here per this pass's own scope.

**Reforge screen amendments (follow-up session — Aspect card sizing, skill-effect hover feedback):**
1. **Aspect-select cards are now fixed-size** (`ASPECT_CARD_SIZE`, both width AND height — the prior bug was width being left content-sized, so "Crimson" vs "Verdant" produced visibly different card widths). `GridContainer` column count is a named constant (`ASPECT_GRID_COLUMNS = 2`), and `ASPECT_COLORS` stays a plain array — a future 5th "Amber" Aspect (a real, documented-but-deferred possibility per the separate design doc's own Open Design Threads) would need zero layout changes, just one more array entry; `GridContainer` wraps into a new row on its own.
2. **Skill effect text now flows through the whole Reforge screen, closing a real server-side gap found by checking first rather than assuming:** `ReforgeSkillOption` (the browse step's wire shape) had no Category/GrantsShield/Description fields at all — confirmed by reading `GameHubModels.cs` directly, not assumed — while `AnimaPartSummary` (the roster's own per-part shape, already used for currently-equipped skills elsewhere) already carried all three. `GameHub.BuildSkillDescription` (a private helper that synthesizes a plain mechanical description — e.g. "Attack · 20 dmg · 2 Energy" — from the skill's own real fields, since `Models.Skill` has no flavor-text field of its own) already existed for `AnimaPartSummary`; `GetReforgeBrowseOptions` now reuses that exact same helper rather than inventing new copy or a second formatting path. Verified end-to-end against the real server (not just compiled) via a throwaway Node.js script that registered a fresh account, named all 3 starters, started a Delve, BFS-walked to a real Reforge node, and confirmed `GetReforgeBrowseOptions` returns real `category`/`grantsShield`/`description` values per skill.
3. **Hover feedback (skill icon + effect text) now appears in three places, all through the SAME docked info bar** (a new `InfoBar`/`InfoIcon`/`InfoLabel` row in `reforge_node.tscn`, styled like every other docked message-area in this codebase — never a floating tooltip, same overflow-risk reasoning Combat's own hover design and Delve's sidebar already established): the skill-browse step's rows (hover-only, since clicking a row already has its own action), the target-picker's "Current: X" line (hover+tap, `MOUSE_FILTER_PASS` so a click still falls through to the card's own "select this target" handler), and the confirm step's Current/New skill names (now two independently-hoverable spans instead of one static "Current -> New" string) — letting the player compare what they're losing against what they're gaining before committing, not just preview the incoming skill. Two wiring helpers cover this: `_wire_hover_only` (mouse-only, for controls with their own click handler) and `_wire_hover` (hover+tap, for purely-informational controls) — same "tap mirrors hover for touch users" convention delve.gd's own skill chips/Artifact rows already use.

Verification for this amendment: headless scene load (zero script errors) plus a synthetic-data pass driving all 4 steps' render paths directly (confirms the new hover-wiring code doesn't error), and the real server round-trip above for the new DTO fields. Live browser click-through was NOT re-attempted this pass, given the session-wide Godot-Web networking degradation already documented immediately above.

## Delve Map Screen Amendments (follow-up session — zoom/scroll, solid node fills, legend) — includes a REAL pre-existing bug fix

**1. Real bug found and fixed: the map's zoom/scroll mechanism has likely never actually worked, since the very first session that built this screen.** `DelveMapScroll` (wheel-zoom) and `_apply_zoom()` (sets `_map_canvas.scale`) were both real, wired-up code — but `MapCanvas` was a DIRECT child of `MapScroll`, a `ScrollContainer`. **Confirmed via a real runtime diagnostic print (not assumed):** `ScrollContainer`, like any Godot `Container`, resets a direct child's `scale` back to `Vector2.ONE` during its own internal layout pass — the print showed `canvas.scale=(2.2, 2.2)` immediately after `_apply_zoom()` ran, then `(1.0, 1.0)` one deferred call later. This is why the map always rendered the entire run flat regardless of the `_zoom` value — not a tuning problem, a structural one. **Fix:** a new plain (non-Container) `MapCanvasWrapper` node now sits between `MapScroll` and `MapCanvas`. `MapCanvas` itself always keeps its natural, unscaled `custom_minimum_size` and only its `scale` changes (untouched by any Container, since it's now a grandchild, not a direct child, of the ScrollContainer); `MapCanvasWrapper` — `MapScroll`'s actual direct child — reports the zoomed size instead, so the ScrollContainer still allocates the correct scrollable range. Re-verified headlessly across multiple frames (`scale` now genuinely persists) and confirmed visually in a real 2560×1440 Web-build session (dragging the scrollbar now reveals real hidden floors; before the fix, the entire 15-floor+Boss run was visible with zero scrolling at any zoom value).

**2. Default zoom retuned against the real bug fix, not the old broken one.** `ZOOM_MAX` raised from 2.0 to 3.0, default `_zoom` set to 2.2 (was 1.0) — both numbers were meaningless under the old broken mechanism (nothing visibly changed at any value) but are now real: 2.2 was confirmed, in the same live session, to require genuine horizontal scrolling to reach the Boss node at 2560×1440, where 1.6 still fit the entire run with room to spare. `ZOOM_MIN` (0.5) unchanged — still lets the player zoom back out to a near-full overview if they want one.

**3. Solid per-type node fills replace outline-only nodes, per a mocked-up direction (not just a verbal description this time).** `DelveMapNode.FILL_COLORS`/`ICON_COLORS` (new, static, on the node script itself — deliberately NOT duplicated in delve.gd the way this codebase's small-mapping convention usually goes, since the whole point of the new Legend is to stay visually identical to the real map, and a second copy could drift out of sync): Combat neutral warm gray (`6e6a62`, no locked color identity of its own, deliberately recedes), Elite muted crimson (`6e2e3a`, ties to Elite's locked `#5a2e3a` room background), Boss deep crimson (`6b1a1a`, the exact locked vignette color), Resource gold (`e8a03a`, reuses the existing Wisp/sparkle amber), Treasure purple (`9a4fc4`, ties to Treasure's locked gem-tone), Shop warm amber-brown (`c9843a`, deliberately warmer/browner than Resource's gold so the two read as distinct at a glance), Reforge bronze/copper (`b87333`). Icon color is picked per type for contrast (light cream on the darker fills — Combat/Elite/Boss/Treasure; dark on the brighter ones — Resource/Shop/Reforge, matching the "current" node's pre-existing dark-icon-on-gold treatment, since Resource's fill IS that same gold). `"current"` state still overrides everything with the old bright-gold fill, unchanged — it needs to stay the single boldest highlight regardless of node type. Icon centering was audited: the math already centered correctly (a computed offset, not guessed), but the previous code used a hardcoded `Vector2(8,8)` magic-number offset that only happened to be correct because `icon_size` was always 16 — now derived from a named `NODE_ICON_SIZE` constant so it can't silently drift if that ever changes.

**4. Hover: scale ~1.15x + brighten, restrained (no glow/blur), restricted to actually-clickable nodes.** `DelveMapNode` now owns its own `mouse_entered`/`mouse_exited` wiring internally (self-contained, no delve.gd involvement needed) and sets `pivot_offset = size * 0.5` so the scale-up happens from the node's own center, keeping the overlaid `IconGlyph` child centered throughout (a Control's children inherit its scale automatically). Deliberately restricted to `INTERACTIVE_STATES` (reachable/selected) — scaling a node the player can't actually click would read as a false affordance signal. Confirmed visually: hovering a reachable node visibly grows and lightens it, icon included, in a real Web-build session.

**5. Legend added (new `MapRow`/`LegendPanel` in `delve.tscn`), and the map area reflows around it rather than leaving a dead zone.** `MapBox` and the new `LegendPanel` (fixed ~210px width) are now siblings inside an `HBoxContainer` (`MapRow`); `MapBox` gets `size_flags_horizontal = EXPAND_FILL` so it takes the remaining width, rather than `MapBox` alone stretching to the full window width the way it did before (the literal cause of the "large unused area to the right" — the map's own PanelContainer background filled the whole width, but its actual rendered content, before the zoom fix, was much narrower). `_build_legend()` populates the Legend by reusing `DelveMapNode` itself at a smaller `view_size` (`LEGEND_BADGE_SIZE = 26.0`, radii stored as ratios of `view_size` rather than absolute pixels specifically so this reuse works) with a new dedicated `"legend"` visual_state — full opacity like `"reachable"`, but never interactive and never dims, since it's a static reference display, not a real node.

**Verification methodology, flagged explicitly:** TODO #18's networking degradation (see above) was still present this session — normal login hung on every attempt, and even a temporary curl-JWT auth bypass (same technique as the Reforge pass) got stuck on "Connecting to GameHub…" for the SignalR handshake itself. Since this task's actual verification target was the Delve screen's real rendering (map zoom/legend/node colors), not the network round-trip (already separately proven to work server-side via a real WebSocket test in an earlier pass), a second temporary bypass was used: `delve.gd`'s own `_ready()` briefly skipped `_run()` entirely and fed `_render_map()`/`_render_team()`/etc. a hand-built but wire-shape-accurate synthetic `DelveStatus` (all 7 node types across 15 floors + Boss, a real 3-Anima roster) — exercising the exact same production rendering code real server data would, just without a live connection. Both this and the login bypass were fully reverted before this diff was considered done (confirmed via `grep` for any remaining "TEMPORARY"/"DIAG"/"bypass" markers). **Incidental fix along the way:** `client/builds/web/serve.js` (the throwaway local static file server used for every Web-build verification pass this project does) had no cache headers at all, and was caught actually serving a stale `index.pck` across two full re-exports mid-session — now sends `Cache-Control: no-store` and strips query strings before resolving the file path. Worth remembering for future verification passes: a "nothing changed" result after a code edit + re-export should be treated as a possible caching artifact first, not just a code failure, given this already happened once.

## Delve Map Screen — Round 2 Readability Pass (zoom re-verification, opaque fills, font baseline, portrait size)

Four fixes from live 2560×1440 testing of the amendments immediately above, this time re-verified against a REAL running server + real SignalR connection throughout (TODO #18 still blocked the real `/api/auth/login` POST, so a curl-obtained JWT was injected directly into `AuthState` — same bypass technique as before — but every screen after that point, including the starter-reveal naming flow and the Delve map itself, ran against the real `Anima.Server`/Postgres, not synthetic data; the bypass was fully reverted, confirmed via grep for "TEMPORARY"/"DIAG"/"bypass", before the final build).

**1. Zoom default re-confirmed at 2.2, NOT silently landing near ZOOM_MAX — the prior report was correct, live-verified numerically this time, not just read off the constant.** A temporary `print("DIAG _apply_zoom: ...")` in `_apply_zoom()` (removed after use) showed `_zoom=2.2 applied_scale=(2.2, 2.2)` in the browser console on real scene entry, and the resulting screenshot showed ~8-9 of 15 floor columns comfortably visible with only a partial scrollbar remaining — matching "several floors visible at once without needing to drag first." No code change was needed here; the live numeric check simply confirms 2.2 is what's actually applied. (If a future report says otherwise again, suspect a stale cached `.pck` first — see the caching-artifact note in the section above — before assuming the constant itself changed.)

**2. Real bug fixed: dim-state nodes were translucent (`modulate.a = 0.45`), not a solid darkened color, which is what actually caused the "washed-out" look** (most visible on Combat's desaturated gray `6e6a62` blending into the screen's dark backdrop). `DelveMapNode._apply_state()` now always keeps `modulate.a = 1.0`; `_draw()` instead darkens `fill_color`/`stroke_color` directly via a new shared `DelveMapNode.dim_color()` static helper (`Color.darkened(DIM_DARKEN_AMOUNT = 0.55)`) when `visual_state == "dim"`. Since the node's icon is a separate sibling `Control` built by `delve.gd` (not drawn by `DelveMapNode` itself), `delve.gd`'s `_build_node_view` now applies the identical `DelveMapNode.dim_color()` treatment to the icon color for dim nodes too, so the icon and its shape stay visually consistent — modulate no longer does any dimming at all, anywhere.

**3. Project-wide font-size baseline introduced: `client/scripts/ui_theme.gd` (`class_name UiTheme`).** Confirmed via search first, per instruction (`find ... -iname "*theme*.tres"` and a `project.godot` grep for `[gui]`/`default_theme` both returned empty) — no global Godot `Theme` resource or project-level font default existed anywhere before this. Rather than introduce Godot's full `Theme`-resource system (which would only matter for controls that DON'T have an explicit per-label override — and nearly everything in this codebase already does, deliberately, per-screen), `UiTheme` is a plain shared constants class with named tiers (`SIZE_MICRO=10`, `SIZE_SMALL=11`, `SIZE_BODY=15`, `SIZE_LABEL=16`, `SIZE_SUBHEADER=18`, `SIZE_HEADER=20`) that any script can reference directly via its global `class_name` (same access pattern `delve.gd` already uses for `DelveMapNode.ICON_COLORS`, no preload needed). This round only migrated the three flagged call sites in `delve.gd` (Legend row labels, Resources panel label+count, Team-row skill-chip labels) from their old literal 10-12px overrides to `UiTheme.SIZE_BODY`/`SIZE_LABEL` — every other existing font-size override in this codebase (HP readouts, position numbers, etc.) was deliberately left alone, since those weren't flagged as hard to read and this was an incremental migration, not a blanket rewrite. Future screens (Shop/Combat) should reach for `UiTheme`'s named tiers instead of inventing new literals. The skill-chip wrapper's own `custom_minimum_size` was bumped 100x16 → 128x22 and its icon 11px → 14px alongside the font bump, so the larger text doesn't clip inside the old, now-too-small hitbox.

**4. Team-row Anima portrait size increased, confirmed Delve-local (not shared).** `delve.gd`'s `_build_team_card` portrait wrapper: `Vector2(40, 40)` → `Vector2(64, 64)`. Checked via `grep` before touching it: this literal is declared independently in `delve.gd` only — `hub.gd`'s own team-row portrait uses a different pattern (`Vector2(0, 90)`, flexible width) and `sanctum.gd`'s roster-card portrait is its own separate `Vector2(56, 56)` literal — so this change cannot have silently resized either of those other screens, and didn't.

**Live verification, explicitly against real data throughout:** real curl-registered account → JWT injected into `AuthState` (bypass, reverted after) → real SignalR connection to `hub.tscn` → real `GetPendingStarterReveal`/`ConfirmStarterAnima` round-trip naming all 3 starters (Marksman/Aegis/Thicket rolled for real, not scripted) → real Hub → real `StartDelve` → real Delve map render, all screenshotted at 2560×1440. Zoomed-in screenshot crops confirmed: node fills read as solid/opaque with no translucent-blend artifact (both the brighter "reachable" and darker "dim" states), Legend/Resources panel text legibly larger, and the 3 team portraits visibly larger without layout overflow.

## Weaving System (LOCKED — real Axie Infinity mechanics as direct inspiration)
Per part slot (Head/Frame/Tail/Crest, all four): pool of 6 candidate genes (D/R1/R2 × 2 parents), weighted 37.5%/9.375%/3.125% → winner becomes offspring's new Dominant; two more rolls fill R1/R2. Mutation: 10%, R1/R2 only. Color: flat 50/50, 100% if same-color parents. Hybrid trigger: both parents pure + correct pairing = 33%. Sibling restriction (copies Axie): only parent-offspring + full-sibling blocked. Weave Count: max 5, cost curve 50/100/175/275/400. Echo: twin-Vessel, 5% spontaneous or guaranteed via 5 Echo Shards, twins are explicitly linked via `EchoTwinId`. Naming is mandatory on every Weave, no exceptions (starter trio uses the same flow). Every materialized Anima (Weave/Boss-hatch/Starter) stores real R1/R2, so 2nd-generation weaving always draws from a real gene pool. Ported to `GameHub`: `AttemptWeave` (commits cost/roll immediately, persists pending state so a disconnect can't lose a paid-for roll) / `ConfirmWeave` (mandatory naming, persists the roster row) / `GetPendingWeave` (reconnect support). (full history: docs/history.md)

## Starter Trio: Real Roll + Naming Flow (NEW THIS SESSION — replaces the hardcoded Ember/Boulder/Sprout trio)

**Retires the last hardcoded-content exception in the whole Anima/Weave/Boss-hatch materialization pipeline.** Previously, every new account's starter trio was 3 fixed `SampleAnimas` instances (Ember/Boulder/Sprout) with hardcoded names and no recorded R1/R2 genome at all — the sole exception to "everything real goes through `AnimaMaterializationService.Build` and gets a mandatory naming prompt." That exception is gone: a fresh account now gets a genuinely rolled trio, structurally identical to a Weave/Boss-hatch Anima, through the exact same mandatory-naming Anima Reveal flow.

**`StarterAnimaService.RollStarterTrio(Random)` (new, `Anima.Core.Weaving`)** — returns 3 `StarterAnimaRoll` (`ArchetypeName`, `AnimaGenome`), fixed presentation order **Crimson/Onyx/Verdant** (Azure deliberately excluded — 3 starter slots, not 4 colors, matching the task brief exactly). Per slot:
1. One of that color's 3 real Archetypes, picked uniformly at random, read straight from `PrimitiveRoster.All` (the same table Reforge/SkillPool/the old hardcoded trio all already draw from — no new mapping invented).
2. **Dominant per part is fully deterministic** — that archetype's own real, already-designed 4-skill set, cloned directly. No randomization happens here at all; only which archetype and R1/R2 (next) are rolled.
3. **R1/R2 per part reuse `BossHatchService.RollOneSkill`** (made `public`, was `private` — the task explicitly asked to reuse rather than duplicate the 55%-toward-body-Color/15%-each-other weighting), so a starter genome's hidden genes are rolled with the identical distribution Boss-hatch already uses.
4. Gen fixed at 1. Since every Dominant is drawn from one single archetype whose own body Color always matches the genome's Color by construction, **`IsFullyPure` is ALWAYS true for a starter roll — not merely probable** (unlike Boss-hatch's ~9.15%). Confirmed structurally identical to a Weave/Boss-hatch genome (real R1/R2, zero special-casing needed in `GenomeFactory`/`AnimaMaterializationService`/`IsFullyPure`) via a new console-harness section (2000-trial statistical checks on archetype-pick uniformity and R1/R2 color distribution, plus a materialization+round-trip check) — same standard `BossHatchService`'s own doc comment holds itself to.
5. Naming default = the archetype name (e.g. "Lotus"), **not** the color name — this is what pre-fills the mandatory naming textbox.

**Rolled at `AuthService.RegisterAsync` time, not lazily on first hub connect** — closes the confirmed gap where a fresh account got zero starter seeding at all (audited and reconfirmed this session: no hardcoded Ember/Boulder/Sprout seeding path exists anywhere in `Anima.Server`, matching the prior session's own "fresh account, empty roster is correct" finding). Persisted immediately as a new `PendingStarterReveal` — same "granted in substance, not yet resolved" DB-backed treatment `PendingWeave`/`PendingBossHatch` already get (`PendingStarterRevealEntity`/`PendingStarterRevealRepository`, EF Core migration `AddPendingStarterReveal`, applied and round-trip-verified against the real dev Postgres container) — so losing the connection before naming all 3 can never orphan a brand-new account with zero playable Anima.

**`GameHub` additions:** `GetPendingStarterReveal()` (reconnect support, returns only the REMAINING unnamed entries — each still carrying its ORIGINAL 1-based `SlotNumber`/fixed `TotalCount=3`, so resuming after naming slot 1 reads "2 of 3", never "1 of 2") and `ConfirmStarterAnima(name)` (confirms whichever slot is current — strictly sequential, no slot-index parameter, matching the client's "1 of 3, 2 of 3, 3 of 3" progress UI; materializes via `AnimaMaterializationService`'s existing Boss-hatch-shaped overload since a starter genome needs the identical no-parents/Gen-1 treatment; advances the index; deletes the whole pending row once all 3 are named). `StartDelve` gained the same "refuse while unresolved" guard `PendingBossHatch` already had, for `PendingStarterReveal` too.

**Auto-team-assign (NEW, follow-up session):** the moment all 3 starter Anima are named, they're automatically set as the active team (`AccountEntity.TeamAnimaIdsJson`) — closes a real gap where a brand-new account had 3 real roster Anima but none marked `InTeam`, so Hub showed "No team selected yet" until the player manually visited Sanctum. This is the ONLY place team assignment ever happens automatically; every subsequent `SetTeam` call (e.g. swapping in a Weave/Boss-hatch Anima later) stays fully manual, unchanged. Implementation: `PendingStarterReveal.ConfirmedAnimaIds` (new field, persisted alongside `NextUnnamedIndex` — migration `AddConfirmedAnimaIdsToPendingStarterReveal`, existing pending rows default to `"[]"`, correct since all of them had `NextUnnamedIndex==0`/nothing confirmed yet, verified against the real dev DB before writing the migration) accumulates each slot's materialized `Anima.Id` as `ConfirmStarterAnima` confirms it; on the FINAL confirm, `Session.TeamAnimaIds` is set from that list and persisted via `accountRepo.SaveTeamAsync` before the pending row is deleted — surviving a disconnect mid-sequence, same durability standard every other `Pending*` entity in this codebase already holds itself to.

**`GenomeFactory`/`Models.Anima` comments updated, not behavior:** `CreateFounderGenome`'s placeholder (keyed off `HeadR1 is null`) is **no longer reachable via any real player's roster** — a real starter Anima now goes through `Build` and has real recorded R1/R2, exactly like a Weave/Boss-hatch Anima. The fallback is kept, unchanged, purely for test/harness code that constructs a bare `SampleAnimas.CreateX()` instance directly (e.g. the console harness's own Weaving distribution checks) — flagged in both files' comments so a future reader doesn't assume it still covers a real starter trio.

**Client (Godot): `starter_reveal.tscn`/`starter_reveal.gd` (NEW) + `hub.tscn`/`hub.gd` (renamed from `post_login.tscn`/`post_login.gd`).** No prior Godot "Anima Reveal component" existed to reuse (Weave/Boss-hatch reveal screens are still only a CLAUDE.md design, never built in Godot) — `starter_reveal.gd` was the first real implementation. Layout: portrait placeholder (color-tinted, real pixel art still deferred at the time, see Hub Screen below for where real Aspect sprites landed instead) → "Starter Anima N of 3" progress → Color/Gen line → Threads section (Dominant always shown, R1/R2 behind a "Show hidden" toggle, default off) → naming textbox pre-filled with the rolled archetype name, editable → Confirm. **`post_login.tscn`/`post_login.gd` renamed to `hub.tscn`/`hub.gd`** (flagged deviation from a literal reading of the task, not a silent one): the task's own brief referred to "hub.tscn" as if it already existed, but the client had no Hub screen at all yet — `post_login.tscn` was always an explicit "you're in" placeholder proving the JWT hand-off + hub connection, exactly the landing point a real hub.tscn would eventually become, so it's promoted/renamed now rather than inventing a second parallel placeholder. `hub.gd` calls `GetPendingStarterReveal()` immediately after connecting, before rendering any real Hub content — any unnamed entries redirect straight to `starter_reveal.tscn`, covering both a brand-new account and a reconnect mid-naming. `login.gd`'s routing needed NO change beyond the renamed constant: both Login and Create Account already routed to the same post-auth scene, so `hub.tscn` itself deciding whether to redirect is sufficient — `login.gd` doesn't need to know or guess anything about starter-reveal state.

**REFACTORED a follow-up session into a genuinely reusable `AnimaRevealPanel` component (`anima_reveal_panel.tscn`/`.gd`), after an explicit audit found it wasn't one yet.** The audit (asked for explicitly, answered by reading the actual code, not guessing): `starter_reveal.gd` fused the generic portrait/Name-Color-Gen/Threads/naming/Confirm DISPLAY together with starter-specific orchestration (the `GetPendingStarterReveal`/`ConfirmStarterAnima` RPC names, the "walk a sequence of slots" loop, the "Starter Anima N of 3" progress format, and the archetype-name default) in the same methods — a future Weave/Boss-hatch reveal screen could not have called into it without forking. Also surfaced: Gen is NOT part of the wire shape at all (`WeaveGenomePreview` has no Gen field for any of the 3 sources), so the reusable component takes `gen: int` as an explicit caller-supplied parameter rather than reading it off the genome — every real caller already knows it locally (Starter/Boss-hatch: always 1; Weaving: `max(parentA.Gen, parentB.Gen)+1`, from the two parent cards already on screen). The refactor was judged cheap (one screen, no dependents yet) and done immediately rather than deferred: `AnimaRevealPanel` now owns ONLY the display + a `confirmed(name)` signal; `starter_reveal.gd` shrank to a thin orchestrator that owns the hub connection/RPC calls and feeds the panel `render(genome, gen, default_name, progress_text)`. `progress_text=""` hides the progress label entirely (for a future Weave/Boss-hatch caller with no multi-slot sequence); `default_name=""` leaves the naming field blank (no archetype to default to for an offspring).

**Verified against the real running server + real dev Postgres container, both ways:** (1) a throwaway Node.js WebSocket script (same "replay the real JSON Hub Protocol handshake, no `/negotiate`" technique the connectivity milestone's own `ws_smoke_test.js` used, deleted after use) — registered a real account, confirmed `GetPendingStarterReveal` returns exactly 3 entries in Crimson/Onyx/Verdant order with real distinct archetypes and real R1/R2 (including at least one off-body-color hidden gene, confirming the 55/15/15/15 roll is really wired through), named all 3 in sequence, disconnected and reconnected after naming 1 of 3 and confirmed it resumed showing "2 of 3" (not a restart), and confirmed the final `SanctumRoster` has exactly 3 rows with the entered names. (2) A throwaway Godot driver script (same spirit as the Login screen session's own driver, deleted after use) drove the REAL `login.tscn` → `hub.tscn` → `starter_reveal.tscn` scenes' REAL signal handlers against the real server for a second, independent fresh account — registered via the real Create Account tab, confirmed the redirect chain, named all 3 slots (confirming the name field pre-fills with the real archetype default each time and the progress label advances 1→2→3 of 3), confirmed landing back on `hub.tscn`, and confirmed a completely fresh `hub.tscn` instance afterward (simulating a reconnect) shows the ordinary placeholder "You're in" state rather than redirecting again. Full 147-check (now 162-check, +15 for the new Starter Trio harness section) console harness reconfirmed passing throughout, no regressions.

## Hub Screen (LOCKED — first real Hub UI, real Aspect + enemy art assets locked)

`hub.tscn`/`hub.gd` (renamed from `post_login`) is filled in for real, matching the supplied mock: header ("Welcome back" + roster-count subtitle), resource bar (Wisp/Echo Shard/Vessel Shard only — no 4th resource, none exists in `ResourceType`), 3 destination cards (Sanctum/Weaving/Collection, all placeholder targets), a team row (real Aspect sprites for non-hybrid Anima, flat color-block fallback for Vulcan/Mirage) + the reusable amber Delve portal (`delve_portal.tscn`, procedural `_draw()`, no image asset), and a "Last delve" summary bar (`GameHub.GetLastDelveSummary`, reads the most-recent `DelveHistoryEntity`, null for brand-new accounts). Always checks `GetPendingStarterReveal()` before rendering. 4 real Aspect sprites + 5 enemy sprites are locked in `client/assets/` — only Aspect sprites are wired (Hub team row); enemy sprites wait on a real Combat screen (see Known TODOs #16). Icons are drawn procedurally via `IconGlyph` (no icon font in Godot). All discovered layout bugs (invisible reveal/team portraits, flat-bar destination cards, cramped card padding) are fixed — root cause each time was a Control with real content but no `custom_minimum_size`. **Known unresolved gap:** Godot Web-exported `HTTPRequest` intermittently times out on `/api/auth/login`/`register` (SignalR/GameHub calls unaffected) — CORS, IPv4/IPv6, and cold-start causes have all been ruled out; root cause still unknown, see Known TODOs #18. (full history: docs/history.md)

## Resource Economy (UPDATED THIS SESSION)
**Persistent:** Wisp, Echo Shards, Vessel Shards. **Ember is NO LONGER persistent** — see Ember & Augment System below.
Run-only: all 12 Artifacts (see below), capped at 3 held simultaneously (see Artifact Cap below). **HP persistence across nodes = attrition** (confirmed).
Reward flow: Resource=30Wisp/no guaranteed Ember (15% chance of 1 bonus Ember), Combat=50Wisp + 1 guaranteed Ember (pickup-choice, see below), Elite=120Wisp + 1 guaranteed Ember with 25% chance each for a 2nd and 3rd (max 3) + **25% chance 1 Vessel Shard (Vessel Shard only, no Echo — REWORKED THIS SESSION)**, Boss=300Wisp + **guaranteed hatched Anima (via Anima Reveal screen, REWORKED THIS SESSION — see Boss Reward & Anima Reveal Screen below) + guaranteed Echo Shard (no longer a 50/50 — Vessel Shard is off the table for Boss now that its old "Vessel" resource grant is retired)**, no Artifact reward at Boss (never was in the Artifact pool).

## Ember & Augment System (LOCKED — not a banked resource)
Ember is a momentary "chance to augment," resolved individually at pickup or purchase (never batched). Drops: Combat 1 guaranteed, Elite 1 guaranteed + 25% chance each for a 2nd/3rd (max 3), Resource 15% chance of 1 bonus. Pickup flow (per Ember): **Augment now** (pick a skill whose own archetype color matches the Ember's color — skill color, not Anima body color — consumes the Ember + a Wisp tier: 15/30/50 for that skill's 1st/2nd/3rd augment slot) or **Convert to Wisp** (flat 15, no augment). Max 3 augments/skill; 4 augment types (Increase Effect +20%, AoE Damage 50%, Decrease Cost -1, Extend +1 Charges) unchanged. Ported to `GameHub`: `PlayerSession.PendingEmbers` is a FIFO queue; `ConvertPendingEmberToWisp`/`AugmentPendingEmber` resolve the front only — DB-backed purchased Embers (Shop) first, then in-memory free (node-dropped) ones. (full history: docs/history.md)

## Artifact Cap (NEW THIS SESSION)
Hard cap of **3 held Artifacts per Delve**, no swap mechanic. Treasure node: if at 3/3 when an Artifact reward triggers, the reward is skipped/lost — no substitute (intentional punish for a wasted node). Boss: unaffected, was never in the Artifact pool. Shop: if at 3/3, the Artifact-for-sale slot doesn't roll that visit.

**Treasure ported onto `GameHub` (`ClaimTreasureNode`):** picks uniformly at random from the full 12-Artifact catalog (no exclusion of currently-held Artifacts — that's a Shop-Wares-only rule, per `ShopService.Roll`'s own comment; a real Delve can duplicate-roll an Artifact already held, same as the pre-existing Delve-simulation reference in `Anima.ConsoleHarness`, which round-robins instead of excluding for deterministic test coverage). Also wires `AccountArtifactStatEntity`'s "discovered" write for the first time (`AccountArtifactStatRepository.RecordDiscoveryAsync`, idempotent, migration-free since it's an existing table gaining a write method, not a schema change) — `GetArtifactCollection`'s `Discovered` flag is now real. `DelvesWonWith` is still always 0 (needs Boss-victory resolution, Phase 5).

## All 12 Artifacts (LOCKED, icons finalized — Sifting Stone added this session)
Twin Flame, Wisp Charm (+20% Wisp), Barrier Stone (+5 Shield team/Round), Vanguard's Bell (+1 Energy Rd1), Weaver's Thread (+1 hand, so hand max becomes 6), Marked Coin (random resource on pickup), Withering Fang (consumed any node, executes lowest-HP to 1HP if combat), Focusing Lens (every 4th Attack = 2x dmg), **Silent Chime (REDESIGNED, follow-up session — fully automatic, no player action/targeting at all: single-use/Delve, the first time any enemy would trigger Enrage while this is held, that activation is cancelled entirely instead — see Silent Chime section below)**, Ember Core (20% Shop discount), Sapling Charm (heal 10% max HP any node, no revive), **Sifting Stone (NEW — at Round end, discard any number of cards from hand before the top-up draw; icon: ti-recycle, no custom shape needed)**.

## Silent Chime (LOCKED — Enrage Interrupt effect)
Fully automatic, no player action/targeting. Single-use per Delve: the first time any enemy would trigger Enrage while Silent Chime is held, that activation is cancelled outright (enemy stays un-enraged for that attempt) and the Artifact is consumed; any later Enrage activation (same enemy next Round, or a different enemy) resolves normally. Implemented as a `DelveRun`-scoped charge (survives across combats in the same Delve; consumption removes the Artifact from `RunLedger.Artifacts`, same pattern as Withering Fang). No `GameHub` method needed — nothing for a player to trigger. (full history: docs/history.md)

## AugmentService (cost model reworked — see Ember & Augment System above)
4 augment types: Increase Effect(+20%) / AoE Damage(50%) / Decrease Cost(-1, unclamped=refund) / Extend(+1 Charges). Max 3/part. Cost is now 1 Ember + Wisp tier (15/30/50), NOT the old flat 2/4/7 Ember-only curve.

## Hand/Deck System (REWORKED THIS SESSION)
- **Deck composition (CLARIFIED THIS SESSION — resolves a prior undocumented gap): 27-card deck = 3 active parts × 3 cards each, per Anima × 3 Anima on the team.** Only Head/Frame/Tail contribute cards — **Crest is passive and contributes zero cards to the deck** (consistent with Crest already being the sole diamond-icon/passive skill type). The deck is fully auto-derived from team selection; there is no separate player-facing deckbuilding screen, and none is needed.
- Hand max: **5 (base), 6 with Weaver's Thread**.
- Hand is persistent — unplayed cards remain in hand at Round end (this was already true in the prior implementation; confirmed, not newly added).
- Round Start draw is a **top-up**, not a full reshuffle: draw count = hand max − cards currently held. This replaced a flat "+3 cards/Round" draw.
- Played cards go to discard. Draw pile reshuffles from discard when empty (already existed, confirmed working under the new model).
- Sifting Stone (see Artifacts) lets the player additionally discard any number of held cards at Round Start before the top-up resolves.

## Energy System (CONFIRMED THIS SESSION — no change made)
+3 Energy at the start of each Round, capped at **9** (not 6 — an earlier in-session assumption of a 6 cap was corrected against the actual tested implementation and intentionally not changed). Vanguard's Bell grants an additional +1 on Round 1 specifically. No other Energy sources currently exist. Energy gates which hand cards are playable; insufficient Energy blocks a card.

## Core Combat Rules
### Colors
| Color | HP | Def | Speed | Dmg mult | Spirit mult |
|---|---|---|---|---|---|
| Crimson(DPS) | 100 | 7 | 10 | 1.3x | 0.7x |
| Onyx(Tank) | 130 | 13 | 7 | 1.0x | ~0.8x |
| Verdant(Healer) | 100 | 10 | 10 | 0.7x | 1.3x |
| Azure(Utility) | 70 | 10 | 13 | 1.0x | 1.0x |

3v3, positions 1(front)/2(mid)/3(back) per side, teams face each other horizontally (player left, enemy right — Axie/STS/Darkest Dungeon-inspired layout). PvE only currently designed; PvP (if ever built) would likely use random placement, deprioritized/not blocking. Any status affecting "target's next action" MUST use Until-Consumed, never Fixed-turn:1.

### Turn Resolution (LOCKED THIS SESSION — Darkest Dungeon-style, not STS batch-commit)
Turn order for the Round is rolled once at Round start (all 6 combatants), shown as a queue. Combatants act **one at a time in that rolled order**, interleaving player and enemy turns — NOT a "queue all your actions, then watch the round resolve" STS-style batch. This was a deliberate choice: the skill kit has heavy reactive/interrupt design (Intercept, Guard Strike, Deflect, Ward, Retribution, Vengeance, Last Laugh) that only makes sense if the player can see what already happened this Round before acting. Enemy intent telegraphing (STS-style "show what the enemy will do before they do it") was explicitly REMOVED given one-by-one resolution — the player sees the real action play out live instead.

Player can **Pass** a given Anima's turn (opts out of playing anything that turn) via an explicit Pass action.

## All 4 Colors — 48 skills, 12 Archetypes (fully coded & tested)
**CRIMSON:** Ember(Slash/Charge/Execute/Reckless), Reaper(Rend/Lunge/Frenzy/Bloodthirst), Marksman(Snipe/Retreat/Marked Shot/Steady Aim).
**ONYX:** Boulder(Bash/Hardened/Taunt/Courage), Aegis(Guard Strike/Fortify/Shatter/Inspire), Warden[sample, distinct from Boss](Intercept/Bristle/Disarm/Vengeance).
**VERDANT:** Sprout(Smite/Guiding Light/Lifebloom/Soul Link), Thicket(Renew/Healing Rain/Bloom/Providence), Lotus(Purge/Silence/Cleanse/Clarity).
**AZURE:** Shade(Pin/Exploit/Misdirect/Ambush), Anchor(Shove/Enfeeble/Hook/Last Laugh), Veil(Deflect/Safeguard/Ward/Retribution).

## Enemy Roster — ALL VALIDATED
Grovehide/Quillfang (Basic). The Sentinel (Elite DPS-check). The Leech Mother (Elite Sustain-check — proved redirect mechanics need genuine targeting ambiguity). **Warden of the Hollow** (Boss, HP220/Def11, FINAL — starter trio has no guard-bypass tool and can't win regardless of stats; ~40% win rate for un-bred teams is intentional).

## ART DIRECTION — Fully Established

**Creature art:** Animas are CONSTRUCTS ("Vessels") — Axie-modular aesthetic (visible seams) over Pokemon-style cohesive design. Deep portrait art is paused/deferred (see top of file); placeholders in use. Warden of the Hollow should visually reference Slay the Spire's Bronze Automaton.

**Tone:** pixel art, cozy+nostalgic but "a bit dark, like STS." **Moonlighter is a genuine structural reference** — both the Delve and Weaving loops were directly modeled on its day/night dual-loop structure.

**THE VISUAL THEME (all core screens):** warm dark "sanctuary/workshop" — `radial-gradient(ellipse at 30% 20%, #4a3a2e 0%, #2b2018 45%, #1a130e 100%)` backdrop, semi-transparent dark cards (`rgba(30,22,16,0.75)`, border `rgba(201,184,158,0.2)`), warm cream text (`#e8cf9a`/`#f0e4d4`), amber accent (`#e8a03a`), horizontal amber-gradient stat bars.

**THE ANIMA LOGO** (also the unofficial game logo / the "enter a Delve" button): glowing amber magic-circle portal — concentric rings, rune-mark dots on the true diagonals, a central diamond gem (**also the symbol for the Crest skill type** — used instead of "star"), and four subtle sigils at the cardinal points, each in a unified dark medallion (deliberately NOT distinctly colored per-color), thin strokes (width 1, opacity 0.7):
- **North = Crimson:** simple inverted-V spike
- **East = Onyx:** open bracket-line, straight centered vertical middle
- **South = Verdant:** trunk + 2 branch offshoots
- **West = Azure:** two parallel curved waves

**Skill-type icon set:** sword=Attack, heart=Heal, shield=Shield-granting, bolt=other Buff, **diamond=passive Crest**.

**"Parts list" pattern** (every Anima card, every screen): portrait + name, then the 4 parts by name+icon, always visible — no hover/click needed.

**Wisp iconography rule (important, applies everywhere):** Wisp is an ethereal/magical resource (will-o'-the-wisp themed name) — NEVER use a coin/currency icon for it, use sparkles/a glowing orb instead.

### The 6 core screens (ALL fully designed, ALL warm-themed) — unchanged this session
1. **Hub** — resource bar top. 3 destination cards (Sanctum/Weaving/Collection). Team (parts-list cards) left + mini Anima-logo portal as Delve button right. "Last delve" summary at bottom.
2. **Weaving** — two **"Strand"** slots (parent-selection term). Wide cards, portrait-left/details-right: name+color+Gen+Weave Count, parts list, and Hidden Threads for ALL 4 parts (including Crest) always visible. Reagent slot (Echo Shards) + Weave Cost readout, mini Weave portal button, result panel at bottom.
3. **Sanctum** — grid of Anima cards (portrait, name, color, Gen, parts list, Weave Count progress bar). Active-team members get an amber border + "In team" badge. Cards link to Profile.
4. **Anima Profile** — portrait (rename icon), color/Gen/Weave Count, parts list, "Threads" section (Dominant per part, dot-accent) with a **"Show hidden" toggle** (matches Axie's real hidden-gene-by-default precedent), a **"Lineage" section** (Parents/Siblings/Echo Twin — all clickable links to that Anima's own Profile), Delve History — **simplified from earlier design-doc language (this session):** lifetime Completed/Failed counters (every delve the Anima has ever been in, not just the last 5) + a compact last-5 list showing only outcome (Victory/Defeat/Retreat) and date. Floor reached/combats won/elites defeated/teammates/Wisp earned are dropped from THIS screen's display scope — those fields still exist on the wire (`DelveHistoryEntry`, see Combat System section below) for a future fuller history view, just not surfaced here.
5. **Delve screen** — map is LARGE/primary focus, horizontal (2 distinct starting nodes per the real algorithm, Boss far right). Real icons: sword=Combat, skull=Elite(bigger), coin=Resource, building-store=Shop, gift=Treasure, hammer=Reforge, crown=Boss(biggest). **Shape encodes risk: Combat/Elite/Boss = circles (sized by threat), safe types (Shop/Resource/Treasure/Reforge) = diamonds.** **Solid per-type fill (UPDATED, see Delve Map Screen Amendments below) — no longer outline-only.** A Legend (right side, same fill+icon convention as the map, smaller) is now part of the locked layout too. Scroll+pinch-zoom (genuinely working as of the same amendments — see that section for a real, now-fixed pre-existing bug). Team (parts-list) left, resources+Artifacts grouped right below the map.
6. **Collection** — top: persistent resource summary with counts+descriptions. Below: "Artifacts (X of 12 discovered)" vertical list — unlocked shows icon+name+description+"Delves won with: X"; locked shows a dim silhouette ("Undiscovered"). Note: count updated 11→12 for Sifting Stone.

### Room-encounter screens — mechanics locked for all 5 node types; visual identity locked for 3, deferred for 2
- **Resource** (LOCKED) — golden radial bg, glowing sparkle/wisp-orb centerpiece, "A quiet cache" flavor text, +30 Wisp shown prominently (+15% chance of 1 bonus Ember, new this session), single "Collect" button.
- **Treasure** (LOCKED) — richer purple/magenta gem-tone bg, "A forgotten chest" flavor text, reveals the actual Artifact offered in a highlighted card (skipped/lost if player at 3/3 Artifact cap), single "Claim" button.
- **Shop** (LOCKED, REVISED THIS SESSION) — warm amber bg (closest to Hub's tone), "A weathered stall" flavor text, now only 2 sections: **Rest** (heal 40% max HP for Wisp) and **Wares**. The standalone "Augment a skill" section was REMOVED — there is no browsable Augment UI at the Shop; Augmenting only ever triggers from acquiring an Ember (combat/elite/resource pickup, or a Wares purchase below). **Wares** sells 3 Ember (random color per slot, duplicates allowed) + 1 Artifact (random from the 12, excluding currently-held, skipped if at 3/3 cap) — each Shop node rolls its own independent stock on entry, no shared pool across multiple Shops in a Delve. Buying an Ember immediately opens the same Augment-page pickup flow used for combat drops. "Leave" link to exit without buying.
- **Reforge** (color-first flow REORDERED + screen visual identity RESOLVED this session — see Reforge section above) — mechanically complete and now ported to `GameHub`: pick Aspect [Crimson/Onyx/Verdant/Azure — the UI's term for color, NOT the old Head/Frame/Tail meaning] → browse-and-pick a skill (all 3 Parts for that color) → pick target Anima (no-op ones skipped) → Accept/Decline. Visual identity: standard warm sanctuary/workshop gradient (no distinct Reforge palette), with a hammer icon accent (small in the header, larger/centered on the Reforge/Leave landing screen).
- **Combat/Elite/Boss rooms** — Combat screen mechanics/layout DESIGNED (see Combat Screen Design below), and **all 3 room BACKGROUND palettes are now implemented and mocked up (LOCKED THIS SESSION)**, background-only swap, everything else on the screen identical across all three: Normal = warm outer gradient (`#4a3a2e`/`#2b2018`/`#1a130e`) + `rgba(0,0,0,0.42)` arena inset; Elite = `radial-gradient(ellipse at 30% 20%, #5a2e3a 0%, #341f2b 45%, #180d12 100%)` + `rgba(20,0,10,0.48)` arena inset; Boss = `radial-gradient(ellipse at 30% 20%, #6b1a1a 0%, #2b0d0d 45%, #0a0505 100%)` + `rgba(0,0,0,0.6)` arena inset + `box-shadow: inset 0 0 120px rgba(139,0,0,0.35)` crimson vignette on the arena panel.

**Shop ported onto `GameHub` (Phase 4):** a real gap surfaced first -- Rest (heal 40% max HP for Wisp) had never been implemented anywhere, even in `Anima.Core` (only mentioned in a comment). Added `ShopService.TryRest`: whole team (matching Sapling Charm's own "heals the whole team" precedent, no target-picker UI exists), no revive (matches the game's general no-partial-death rule), `RestWispCost = 40` -- a first-pass number (like every other undetermined Wisp amount in this codebase), not a locked design decision. `GameHub.EnsureShopVisited` rolls Wares' stock exactly once per node visit (cached in a new in-memory `ShopVisitState`, not re-rolled on repeat reads) and fires `ArtifactService.OnNodeVisited` exactly once too, regardless of whether Rest or Wares is the player's first action. `BuyWaresArtifact` wires the same `AccountArtifactStatRepository.RecordDiscoveryAsync` `ClaimTreasureNode` already uses (Phase 3) -- one shared method, two call sites, not duplicated logic.

**Ember-purchase persistence gap (found + fixed, Phase 4 audit):** `PlayerSession.PendingEmbers` (Phase 3) is in-memory-only, justified because a FREE node-dropped Ember has no stored value to lose on disconnect. Buying an Ember from Wares breaks that justification -- `EmberService.TryBuyEmber` spends real Wisp immediately, before the Augment/Convert choice is ever made, the same "paid, resolution pending" shape as the pending-Weave bug. Fixed with a new `PendingPurchasedEmberEntity`/`PendingPurchasedEmberRepository` (EF Core migration `AddPendingPurchasedEmber`, applied and round-trip-verified against the dev Postgres container, including FIFO ordering across a simulated reconnect) -- NOT unique per account, unlike `PendingWeaveEntity`, since a single Shop visit can sell up to 3. `ConvertPendingEmberToWisp`/`AugmentPendingEmber` now resolve the paid queue's oldest entry first, falling back to the free in-memory queue only once the paid one is empty; `GetPendingEmberColorsAsync` reports both merged as one combined front-to-back list, so the client's "resolve the next one" loop needs no awareness of the split.

### The 12 Artifact Icons (finalized, Sifting Stone added this session)
Twin Flame=flame, Wisp Charm=sparkles, Barrier Stone=shield, Vanguard's Bell=custom clean bell outline (no clapper), Weaver's Thread=custom 3 slanted diagonal lines, Marked Coin=custom coin outline + sparkle-star inside, Withering Fang=custom sharp pointed tooth shape (NOT a stock Tabler icon — `ti-tooth` does not exist, must be a custom SVG shape), Focusing Lens=custom magnifying glass (no plus sign), Silent Chime=asterisk (placeholder), Ember Core=sun, Sapling Charm=leaf, **Sifting Stone=ti-recycle (stock Tabler icon, no custom shape needed)**.

## Combat Screen Design (fully designed; Phase 1 — static layout + real-data rendering — now implemented in Godot, see Combat Screen — Phase 1 Implementation below)

**Layout, top to bottom:**
1. HUD row: Round counter (also shows node type, e.g. "Round 3 — Combat"), Wisp count (sparkle icon, never a coin), Artifacts row (icons, hover for description — see tooltip pattern below).
2. Divider line.
3. **Arena**: darker inset panel (`rgba(0,0,0,0.42)` over the base gradient) clearly separated from the rest of the HUD — this was an explicit request to make the arena "pop" visually. Player team left, enemy team right, no "VS" divider text. Each side's 3 Anima/enemies arranged front-to-back with front nearest the center gap (position 1=front closest to center, 2=mid, 3=back, matching the existing position table).
4. Each combatant card: portrait, name, HP bar (color-coded: green >50%, amber/gold 25–50%, red ≤25%), HP numbers, a buff/debuff icon row (FIXED HEIGHT reserved always, even when empty, so...), a **position-number line (1/2/3) that sits at the same vertical height for every combatant regardless of buff count** — this required reserving fixed height for the buff row rather than letting it collapse when empty. Dead/removed combatants show as an empty slot at the same total height (position held, portrait gone), with a darkened line and dim position number — no dashed placeholder box.
5. The currently-active combatant's position line is highlighted gold, tied to the turn order queue's highlighted entry.
6. Divider, then a **message area** (NOT a floating tooltip, NOT a top banner) — a single docked line of text used for THREE things: (a) buff/debuff and Artifact hover descriptions, (b) the "choose a target" instruction when a targeted card is selected, (c) the Confirm/Cancel prompt before a card resolves. This consolidation was deliberate — floating tooltips/popups positioned near edge-of-screen elements (e.g. the rightmost enemy, or the Artifacts row in the top-right corner) kept overflowing the viewport with center- or edge-anchored CSS; a single area that's part of normal document flow cannot overflow by construction. Hovering something temporarily overrides the message area's content and reverts to whatever the "current state" message was (targeting instruction, confirm prompt, or blank) on mouse-leave.
7. Divider, then: turn order queue (LEFT column, vertical list of full names — not initials — showing the FULL Round's rolled order, gold left-border-accent for player-side entries, coal-gray (`#8a8175`, matching enemy portrait color) for enemy-side, current/next actor highlighted) alongside Energy pips + Deck/Discard counts + Pass button (all in one row) + the hand of cards.
8. Hand cards: vertical card layout, cost pip (circle, color-coded to the skill's archetype color), placeholder art block, skill name, effect text. Unaffordable cards (insufficient Energy) are dimmed with a red X overlay and not clickable. Hand max is 5 (6 with Weaver's Thread) — deck/discard counts sit inline with Energy (not as a side column next to the hand) specifically to leave room for a 6th card without crowding.
9. Combat log: bottom of screen, scrollable (deliberate exception to the general "no nested scrolling" UI convention used elsewhere — a combat log needs full history, not just the last few lines), logs ALL combat events including AI/enemy turns and system events (Round starts, Enrage escalating), color-coded by event type (damage/heal/shield/neutral).

**Targeting/confirm flow:**
- Clicking a hand card: if self-targeting, goes straight to the Confirm prompt (in the message area) using the acting Anima as the implicit target. If it needs an enemy/ally target, outlines ONLY the legal target set on the arena (not all combatants) and shows a "choose a target" instruction in the message area.
- Clicking a legal target opens the Confirm prompt in the message area: "Play [skill] on [target]? Confirm / Cancel." Nothing resolves until Confirm is pressed. Cancel fully backs out of targeting mode. This confirm step was explicitly requested as a misclick-prevention measure.
- On Confirm: a floating +/- number rises and fades above the target's portrait, plus a brief colored flash on the portrait (red=damage, green=heal, silver-blue=shield). IMPORTANT implementation note: in the real client this feedback must fire only after the server/combat engine confirms the action's actual resolved result (including anything a reactive skill like Deflect/Vengeance changes), not optimistically on click — the mockup fires it optimistically only because there's no real backend behind it.

**Enrage display:** shown as a buff/debuff icon (flame) in the affected combatant's own status row, NOT a separate standalone HUD badge — this matters because Enrage is per-combatant (typically Boss/Elite), not a screen-wide state.

**Hover tooltips (buffs/debuffs, Artifacts):** originally floating positioned popups, retired in favor of the docked message area (see point 6 above) specifically because floating tooltips centered on edge-of-screen elements overflowed the viewport and a per-element edge-detection fix (right-align near the right edge, left-align near the left edge) proved fragile in testing. The docked message area is the intended real pattern — worth reusing anywhere else in the UI that has this same edge-tooltip-overflow risk (Collection's Artifact list, Sanctum cards, etc.).

**Formerly deferred here, now resolved (stale cross-reference cleaned up this session):** the win/loss transition and Elite Ember-pickup placement questions that used to be listed as "not yet designed" were both resolved by the Match Result & Retreat System section below — see there instead.

## Combat Screen — Phase 1 Implementation (static layout + real-data rendering, no player interaction yet)

`client/scenes/combat.tscn`/`combat.gd` (NEW) — same child-panel/shared-connection architecture as Resource/Treasure/Reforge/Shop, instanced into `delve.gd`'s `NodeEncounterOverlay`. Deliberately scoped to layout + rendering only: no card click/targeting, no `GetLegalTargets`/`SubmitAction` calls at all, no Confirm/Cancel flow, no hit-feedback animations, no hover tooltips, no live combat-log streaming beyond whatever `StartCombat`'s own initial response carries, no Enrage icon logic (status keywords render as plain text chips, no per-keyword icon mapping yet). That real interactive loop is Phase 2+.

**RPC names/shapes confirmed directly against `GameHub.cs`/`GameHubModels.cs` — unlike Shop/Reforge, CLAUDE.md's own prior prose (`StartCombat`/`SubmitAction`/`GetLegalTargets`/`GetCombatState`) turned out to be accurate as written this time.** Real shapes used this pass: `CombatStatus(RoundNumber, SharedEnergy, PlayerTeam[CombatantSummary], EnemyTeam[CombatantSummary], Hand[HandCardSummary], DrawPileCount, DiscardPileCount, TurnOrder[CombatTurnEntry], TurnIndex, CurrentActorAnimaId, Outcome, EventLog[string], VictoryReward?, BossHatchPreview?, DefeatSummary?)`. `StartCombat()` is idempotent — a repeat call for a still-uncleared node just returns the existing `ActiveCombat` state, confirmed live (re-opening the same Combat node resumed the identical Round/HP/hand/turn-order, with `EventLog` correctly empty on the resume — only StartCombat's very first real call carries log lines, exactly matching `BuildCombatStatus`'s own `log ?? Array.Empty<string>()` default).

**Node-type background tier is passed in explicitly by `delve.gd`** (`start(hub, node_type)`, `node_type` = `"Combat"`/`"Elite"`/`"Boss"`) — `CombatStatus` carries no node-type field at all, so the client has no way to read it off the wire; the caller already knows `CurrentNode.Type` from routing here in the first place, same as every other node screen's own dispatch. All 3 locked background palettes (Normal/Elite/Boss gradients + arena-inset colors + Boss's crimson vignette) are implemented in `combat.gd`'s `_apply_background_tier` — this is their first real Godot implementation (Combat had no client screen before this pass). **Only the Normal (Combat) tier was live-verified this pass** — the new DEV button only forces a Combat node (per this pass's own scope); Elite/Boss aren't independently forceable without their own DEV buttons, which weren't requested, so those two tiers are code-reviewed but not live-screenshotted.

**Real, confirmed wire-shape gaps found, worked around rather than silently patched:**
1. **`CombatantSummary` has no `AnimaId` field** — no direct way to map a Player-side combatant back to its Anima's Color (needed for its Aspect sprite) by index alone, since `CombatStatus.PlayerTeam`'s index order (mirrors `DelveRun.Team`'s `StartDelve` `TeamAnimaIds` order) is not guaranteed to match `GetRoster()`'s own order (`Session.Roster.Animas` insertion order — confirmed these can legitimately differ by reading `GameHub.cs`). Worked around by matching on `Name` instead (fetches the real team roster via `GetRoster()`, filtered to `InTeam`, and looks up each `CombatantSummary.Name` against it) — correct for any real team in practice (names aren't normally duplicated within one 3-member team) but not a strict identity guarantee. Flagged for Phase 2: if this ever becomes a real problem, add `AnimaId` to `CombatantSummary` directly, mirroring `HandCardSummary`'s own `OwnerAnimaId`.
2. **`HandCardSummary` has no description/effect-text field** — unlike `AnimaPartSummary`/`ReforgeSkillOption` (both carry a server-synthesized `BuildSkillDescription` string), `HandCardSummary` was never given the same treatment. This pass substitutes a plain `"<Category> · <EnergyCost> Energy"` caption from the fields that ARE present — a real, lesser substitute for actual effect text. Flagged for Phase 2: add a `Description` field to `HandCardSummary`, reusing the existing `BuildSkillDescription` helper, same as every other skill-bearing DTO already does.

**Real, significant CombatEngine/materialization gap found (not a client bug — confirmed via source, then confirmed live):** every real materialized Anima (Weave/Boss-hatch/Starter) is created with `Position` hardcoded to `1` (`AnimaMaterializationService.cs:73`), and nothing in `StartCombat`/`DelveRun.Start` ever assigns distinct 1/2/3 (front/mid/back) positions across a real team before a fight begins — `Position` only ever changes via in-combat move effects (Shove/Retreat/Hook offsets) or Summon placement. **Live-confirmed**: a fresh Round 1 against a freshly-started Delve showed all 3 real player Anima (Aegis/Marksman/Thicket) rendering Position 1/1/1 simultaneously. This isn't cosmetic — position-dependent skill logic (e.g. Steady Aim's `Position == 3` check) can never trigger for any player Anima on a genuinely fresh team until something else moves one of them first. The arena's front-to-back rendering in `combat.gd` is correctly displaying the REAL wire data as-is; the gap is upstream (nothing assigns initial team positions), not in this screen. Flagged here rather than silently worked around, since a client-side guess at "real" positions would mask a real server-side gap — worth a decision on whether `DelveRun.Start`/`StartCombat` should auto-assign 1/2/3 by team order before this is revisited.

**Enemy sprites finally wired (TODO #16 closed):** `ENEMY_SPRITES` in `combat.gd` maps the 5 real, fixed `Enemy.Name` strings (confirmed via `SampleEnemies.cs`: `"Quillfang"`, `"Grovehide"`, `"The Sentinel"`, `"Leech Mother"`, `"Warden of the Hollow"`) directly to the 5 real sprites already sitting unused in `client/assets/enemies/`. A future 6th enemy with no matching entry falls back to a plain darkened tile, same graceful-fallback spirit as `HYBRID_FALLBACK_TINTS` on the player side, not a hard error.

**New DEV button (5th):** `DevForceCombatButton` in `delve.tscn`'s debug row, same BFS-to-nearest-matching-node-type mechanism the other four already use. **New DEV-only "Back to Map" button on `combat.gd` itself** (not part of the locked design) — Phase 1 has no real way to leave Combat otherwise (that's Phase 2's Victory/Defeat flow); pressing it only tears down the client-side panel (emits `resolved`, same convention every node screen uses) without any server call, so the real `ActiveCombat` state is untouched and resumable. Strip both DEV additions once Combat's real interactive loop (win/loss included) ships.

**Verification, explicitly against real data:** TODO #18 still reproduced (real `/api/auth/login` blocked, "Could not reach the server" — confirmed via a real registration attempt in-browser before falling back), so the established JWT-bypass technique was used (real account registered via curl, JWT injected into `AuthState`, fully reverted and re-exported clean afterward — confirmed via grep). From there, everything was real: real starter-trio roll/naming (Marksman/Aegis/Thicket), real Hub, real `StartDelve`, real `MoveToNode` to Floor 1's fixed Combat node, real `StartCombat` against a real rolled Combat encounter (Grovehide + Quillfang). Confirmed rendering correctly: real HP bars (green, full health), real turn order matching the real combat-log line (`Thicket(Spd 10) > Marksman(Spd 10) > Aegis(Spd 7) > Grovehide(Spd 5) > Quillfang(Spd 5)`), real Energy/Deck/Discard counts, real hand cards (names, per-skill-color cost pips correctly tinted per each card's own archetype color, not the owning Anima's body color), real Aspect sprites on the player side (name-matched) and real enemy sprites on the enemy side, and the idempotent-resume behavior described above.

**Verification gap, honestly flagged:** a literal pixel-perfect 2560×1440 screenshot was NOT achieved this pass — the browser-automation tool's effective content viewport stayed capped at roughly 1278×1270 regardless of the requested window resize (confirmed via `window.innerWidth`/`outerWidth` — `outerWidth` genuinely reported 2560 while `innerWidth` stayed ~1278, and resizing the window larger failed outright since the physical display is only 2560×1440), a fixed overhead in this specific tool/environment, not a code issue. What WAS confirmed at the available width: the `CenterContainer` + fixed-width (1400px) `Card` technique correctly does NOT stretch-to-fill — the panel stayed at its fixed width rather than sprawling edge-to-edge, the same anti-stretch mechanism already confirmed working at a genuine 2560×1440 for Reforge/Shop in an earlier session. A literal wide-viewport screenshot of Combat specifically is still outstanding.

**One console error observed, traced to the temporary bypass, not to Combat:** `ERROR: Parent node is busy adding/removing children, remove_child() can't be called at this time.` reproduced on the very first engine boot, before the Combat screen (or even the real Delve flow) was ever reached — traced to the temporary bypass's `_ready()`-time synchronous `change_scene_to_file` call racing the engine's own scene setup, a known Godot-Web quirk of that specific test-only scaffolding, not a defect in `combat.gd` or `delve.gd`'s production code paths. The bypass was fully reverted before this pass was considered done.

## Combat Screen — Phase 2 (turn-gated hand, card ownership, Position fix — follow-up session)

Three targeted fixes on top of Phase 1's static layout, all live-verified against a real fight.

**1. TODO #22 fixed — real, distinct 1/2/3 team positions.** Root cause (per Phase 1's own finding): every materialized Anima's `Position` defaults to a hardcoded `1` (`AnimaMaterializationService.cs:73`), and nothing ever assigned distinct front/mid/back positions before a fight. Fixed in `GameHub.StartCombat()` (NOT `DelveRun.Start()`): right after `CombatState` is built (and after the idempotent-resume early-return, so a genuine reconnect never resets a move-effect's real mid-fight position), `for (var i = 0; i < state.PlayerTeam.Count && i < 3; i++) state.PlayerTeam[i].Position = i + 1;`. Deliberately reassigned fresh on EVERY genuine new fight, not just once at Delve start — this is the self-healing choice: a prior fight's in-combat Shove/Retreat/Hook moves (which do mutate `Position`) can never leak into the next fight's starting formation this way. Order comes from `run.Team`'s own list order, i.e. whatever order `StartDelve`'s `TeamAnimaIds` was submitted in. **Investigated and answered the "Sanctum team-slot order vs. a new ordering concept" question the task posed:** `SetTeam` is a real, fully working RPC, but grepped the whole `client/` tree and confirmed it is never actually called from anywhere in the Godot client — Sanctum has no team-builder UI yet (view-only, "In team" badges only). The ONLY thing that currently ever sets `TeamAnimaIds` is the starter-trio auto-assign (`ConfirmStarterAnima`'s final confirm), in fixed Crimson→Onyx→Verdant confirmation order. No new ordering concept was needed — team list order (whatever produced it) is already a perfectly sufficient front/mid/back signal, and the mechanism generalizes correctly the moment a real Sanctum team-builder starts calling `SetTeam` with a deliberate order. **Live-verified**: a fresh 3-Anima team (Ember/Warden/Lotus, confirmed via `TeamAnimaIds` order Crimson→Onyx→Verdant) rendered Position 1/2/3 respectively — arena showed Lotus(3)/Warden(2)/Ember(1) left-to-right, matching the locked front-nearest-center layout.

**2. Card ownership labels — no server change needed.** Re-confirmed by re-reading `GameHubModels.cs`: `HandCardSummary.OwnerAnimaId` already existed on the wire before this pass (unlike the still-open missing-Description-field gap) — it was just never rendered. `combat.gd`'s `_build_hand_card` now shows a small color-swatch (the skill-color hex tints, same `SKILL_COLOR_TINTS` dict) + owner name chip per card, resolved via a new `_team_by_id` dict (`{animaId: {name, color}}`, built from `GetRoster()` alongside the existing name-keyed dict Phase 1's sprite-mapping workaround already uses) — a REAL id-based lookup this time, not the name-matching workaround the arena portraits still need (arena still can't do this, since `CombatantSummary` still has no `AnimaId` — that gap is unchanged, still flagged for a future pass). **Live-verified**: hand cards correctly labeled "Ember"/"Warden"/"Lotus" matching each card's real owner, distinguishing e.g. two different Anima's same-named or different skills at a glance.

**3. Turn-gated hand — two visually distinct dim states, plus Pass wired for real.** `_build_hand_card` now computes `is_owner_turn` (`CombatStatus.CurrentActorAnimaId == card.OwnerAnimaId`) and `affordable` (`EnergyCost <= SharedEnergy`) per card: a non-active-Anima's card gets a flat, colorless dim (`modulate.a = 0.4`, same family as the arena's dead-combatant treatment) and is NOT clickable; an active-Anima's-but-unaffordable card gets a lighter dim (`0.55`) PLUS a red "✕" overlay (the locked design's own "dimmed with a red X overlay" language, implemented for the first time this pass — Phase 1 had skipped it entirely despite the design doc mentioning it); a fully eligible card (active Anima's + affordable) renders at full opacity with a pointing-hand cursor and a real `gui_input` click handler. **Deliberately still NOT real targeting/play**: clicking an eligible card does not call `SubmitAction` with a real `HandIndex`/`Target` (that's real Phase 3 scope — `GetLegalTargets`, the Confirm/Cancel flow) — it only surfaces an honest placeholder in the message area ("Would play X — targeting/confirm not implemented yet (Phase 3)."), so the interactive-ready affordance is real without pretending a flow exists that doesn't. **Pass button wired for real** (`SubmitAction` with `HandIndex`/`Target` both null, per `SubmitActionRequest`'s own "HandIndex null = Pass" convention) — added specifically because this task's own live-verification ask ("confirm... as turn order advances through a few turns") had no way to be fulfilled otherwise; Phase 1 left Pass permanently disabled since nothing could exercise it. Pass is now enabled only while `CurrentActorAnimaId` is non-null and no request is in flight. **Live-verified across a full cycle**: Ember's turn (Ember's cards bright, Warden/Lotus dimmed) → Pass → Lotus's turn (Lotus's card bright, others dimmed) → Pass → Warden's turn (Warden's card bright) → Pass → enemies auto-resolved (Grovehide/Quillfang both attacked, real damage logged) → Round 2 began fresh on Ember's turn again, Energy correctly incremented to 9 (capped). Every transition correctly re-gated the hand to the new current actor.

**Real-data verification methodology**: same JWT-bypass technique as Phase 1 (TODO #18 still blocks real login), fully reverted afterward (confirmed via grep) — a temporary diagnostic `print()` in `_render_hand` was also added mid-pass to rule out a one-off dimming discrepancy seen on the very first Pass click, confirmed via console log that `CurrentActorAnimaId`/`TurnIndex`/`TurnOrder` were all real and mutually consistent, and a repeat test (fresh page load, fresh fight) reproduced correctly every time afterward — the print was removed before this pass was considered done. **One honestly-flagged, unresolved curiosity, not chased further**: the diagnostic print's console output showed each real state change logged TWICE in immediate succession with identical (always correct) data — the same unexplained "double render/replay" phenomenon Phase 1's own verification notes already flagged once before under different circumstances. Harmless every time observed (never divergent data between the duplicate pair), but still unroot-caused; worth keeping in mind if a future pass hits something that looks like a double-fired signal or duplicate RPC call.

**Still not exercised live this pass**: the new red-"✕"-overlay unaffordable state — every skill seen in testing cost ≤2 Energy against a 6-9 Energy pool, so nothing naturally became unaffordable. Code-reviewed only (same `energyCost`/`sharedEnergy` fields already proven correctly read elsewhere), not live-screenshotted.

## Combat Screen — Phase 3 (real targeting, Confirm/Cancel, real card play — follow-up session)

**Audit findings (read first, per this pass's own instructions — done via `GameHubModels.cs`/`GameHub.cs`/`CombatEngine.cs`, not assumed):**
1. **`SubmitActionRequest.Target` is a `CombatantRef(Side, Index)`** — the exact same identity scheme `CombatantSummary` already carries as its own `Side`/`Index` fields (confirmed via `CombatantRef`'s own doc comment: "the one identity scheme that works for both sides uniformly"). **This means real targeting needs no `AnimaId` at all** — the long-open "`CombatantSummary` has no `AnimaId`" gap (TODO #23, still open) turned out to be irrelevant to targeting; it only ever mattered for the arena's Player-side sprite-color lookup (a separate, still-open concern, unchanged by this pass).
2. **`HandCardSummary.TargetType`** (already on the wire) is the skill's raw `TargetType` enum name as a string (`"SelfTarget"`, `"Enemy"`, `"LowestHpEnemy"`, `"Ally"`, `"LowestHpAlly"`, `"ChosenEnemy"`, `"ChosenAny"`, `"AllEnemies"`, `"AllAllies"`) — so target-type is directly exposed per card; no need to infer it by calling `GetLegalTargets` first and reasoning about the result shape, as the task's own framing had anticipated might be necessary.
3. **`GetLegalTargets(handIndex)`** (confirmed by reading `CombatEngine.GetLegalTargets` directly): `SelfTarget` → returns exactly `[actor]` (**not** empty); `AllEnemies`/`AllAllies` → returns `[]` (genuinely empty, no explicit target possible); every other `TargetType` (including algorithmically-resolved ones like `Enemy`/`LowestHpEnemy`/`Ally`/`LowestHpAlly`, which pick a single winner automatically) returns a **real list** — even a single-option list is returned as a list, per the engine's own comment, specifically so the client's "highlight the legal set, then click" flow works uniformly across every non-self, non-AoE skill. **Consequence for the client:** "skip straight to Confirm" is only correct for `TargetType == "SelfTarget"` (checked via the wire field, not by checking whether the returned list happens to have length 1) and for the genuinely-empty AoE case — every other case, including a skill that algorithmically resolves to a single legal target, must still go through the real highlight-and-click step.
4. **Real discrepancy found in `SubmitActionRequest`'s own doc comment, worked around correctly rather than trusted at face value**: the comment claims "`Target` ... null if that set came back empty (`SelfTarget`/`AllAllies`/`AllEnemies` skills need no explicit target)" — but `SubmitAction`'s actual validation (`legalTargets.Count > 0 && (explicitTarget == null || !legalTargets.Contains(explicitTarget))`) means a `SelfTarget` skill's non-empty `[actor]`-only list **does** require an explicit, non-null `Target` matching the actor, contradicting that comment. Confirmed live: an early test that (incorrectly, before this was understood) sent `Target: null` for a self-targeted skill was rejected. The client now always sends the resolved `CombatantRef` explicitly for `SelfTarget`, and only sends `null` when `GetLegalTargets` genuinely returned an empty list (true AoE).

**Implementation (`combat.gd`, `combat.tscn`):** a 3-state phase machine (`PHASE_NONE`/`PHASE_CHOOSING_TARGET`/`PHASE_CONFIRMING`). Clicking an eligible hand card calls `GetLegalTargets`, then either jumps straight to a Confirm prompt (`SelfTarget`, or a true AoE with an empty list) or enters `PHASE_CHOOSING_TARGET`, which outlines *only* the legal arena cards (a 2px amber border wrap, not a full recolor — per the locked design's "outline ONLY the legal target set" language) and disables everything else (other hand cards, Pass). Clicking a highlighted target (or having skipped straight there) shows `"Play [skill][ on [target]]? Confirm / Cancel"` in the message area via two new `ConfirmButton`/`CancelButton` nodes added to `MessageRow` in `combat.tscn`. **Cancel is shown during BOTH `PHASE_CHOOSING_TARGET` and `PHASE_CONFIRMING`** — a small, deliberate addition beyond the locked design's literal wording (which only describes Confirm/Cancel at the confirm step): without it, a misclick into targeting mode on the wrong card would have no escape hatch short of finishing some other action. Confirm calls `SubmitAction` with the real `HandIndex`/`Target` and re-renders the whole screen from the returned `CombatStatus`, the same re-render path Pass already used.

**A real, confirmed engine-level bug was found and fixed during live verification, not just a client logic bug:** the first implementation called `_render_hand()`/`_refresh_interactive_ui()` **synchronously**, before any `await`, from directly inside the very hand-card's or arena-card's own `gui_input` signal handler that triggered them. `_render_hand()`/`_render_arena()` free-and-rebuild every child every call (this codebase's established rebuild convention) — including the node whose `gui_input` emission was still on the call stack. Godot does not allow freeing an object that's mid-emit: this reproduced **on every single click**, logged as `ERROR: Object is locked and can't be freed.` / `SCRIPT ERROR: Attempted to free a locked object (calling or emitting).` with a real GDScript backtrace confirming the exact call chain (`_render_hand → _begin_targeting → _on_hand_card_gui_input`, and `_render_arena → _refresh_interactive_ui → _on_arena_card_gui_input`). Silently swallowed by the engine (the node simply wasn't freed), this corrupted later state — a confirmed real consequence, not theoretical: a `Play Guard Strike on Grovehide?` Confirm actually submitted `Target: {Player, index 1}` (Aegis targeting itself) instead of the real chosen `{Enemy, index 0}` (Grovehide), which the server correctly rejected as an illegal target. **Fix:** the two pre-await render calls in `_begin_targeting` and the synchronous render call in `_on_arena_card_gui_input` now go through `call_deferred(...)` instead of a direct call, so the actual free/rebuild runs after the triggering signal's own call stack has fully unwound. Re-verified live after the fix: zero "locked object" errors across a full re-test of both a self-targeted and a non-self-targeted play.

**Live-verified against the real running server** (fresh account registered via curl, JWT injected into `AuthState` via a temporary bypass in `login.gd`'s `_ready()`, fully reverted and re-exported clean afterward — confirmed via grep for "TEMPORARY"/"DIAG"/"bypass"/the literal token): real starter-trio roll (Marksman/Aegis/Sprout), real Hub, real `StartDelve`, DEV-forced Combat node, real `StartCombat` against Grovehide/Quillfang. **Self-targeted skill** (`Guiding Light`, `TargetType.SelfTarget`): clicking it skipped straight to `"Play Guiding Light on Sprout? Confirm / Cancel"` with no target-picking step; Confirm produced a real combat-log resolution (`Sprout's Soul Link triggers... Heal: 20 base x 1.3 mult = 32 healed`) and correctly advanced the turn. **Non-self-targeted skills** (`Guard Strike`/`Smite`/`Shatter`, all `TargetType.Enemy` against 2 living enemies): clicking highlighted **only** the front-position enemy (Grovehide), never both — confirming the legal-target restriction is real, not a "highlight everything" bug; selecting it and confirming produced real damage log lines and correctly updated HP bars/hand/turn order/Energy. Re-tested across a full Round 1 → Round 2 transition: Energy reset, hand top-up, and a real enemy counter-attack (Grovehide's Claw Swipe on Marksman, HP 100→82) all reflected real server state with no client-side guessing.

**New, unrelated, honestly-flagged finding (not investigated further, out of scope for this pass):** during the same verification session, a fresh page reload once logged `ERROR: Parameter "data.tree" is null.` / `Invalid access to property or key 'process_frame' on a base object of type 'null instance'` with a backtrace pointing at `hub.gd:307`'s `_connect_hub` (an `await get_tree().process_frame` call). This is in `hub.gd`, unrelated to any Phase 3 combat.gd change, and didn't block anything — the connection succeeded moments later regardless (`GetRoster`/`StartDelve` proceeded normally). Flagged here for whoever next touches Hub's connection-retry path; not chased further since it's outside this pass's scope.

**TODO status:** #24 (real targeting/Confirm/Cancel/`SubmitAction` with a real `HandIndex`/`Target`) is now resolved — see above. #23 was only *partially* narrowed as of this pass: (a) `CombatantSummary` still has no `AnimaId` (still open, still only needed for arena sprite-color lookup, confirmed unrelated to targeting) and (b) `HandCardSummary` still has no description field (still open at the time of this Phase 3 write-up — resolved two sessions later in Combat Screen Phase 5 below).

## Combat Screen — Phase 4 (hit-feedback: floating +/- numbers, portrait flash — follow-up session)

**Real wire-shape gap found and closed: `CombatantSummary` had no numeric Shield value at all.** `Statuses` was (and still is, for every OTHER status) keyword-only by original design ("add magnitude if/when the client needs it" — see `GameHubModels.cs`'s own comment). This pass's diff needs a real number to detect "Shield rose," not just "the Shield keyword is present," so a single `Shield` int field was added to `CombatantSummary` (`GameHubModels.cs`) and populated in `GameHub.BuildCombatStatus`'s `ToSummary` via `c.ActiveStatuses.FirstOrDefault(s => s.Keyword == "Shield")?.Magnitude ?? 0` — a one-field addition, not a general per-status magnitude map, since Shield is the only status any real client need has touched so far.

**Doc-comment fix (also requested this pass):** `SubmitActionRequest`'s comment previously claimed `Target` may be null whenever `GetLegalTargets` "came back empty (SelfTarget/AllAllies/AllEnemies skills need no explicit target)" — Phase 3's own audit had already found this wrong (`GetLegalTargets` returns a non-empty `[actor]`-only list for `SelfTarget`, and the server's real validation requires an explicit Target in that case). The comment now correctly states only `AllAllies`/`AllEnemies` fall into the "null is fine" case; `SelfTarget` does not. No behavior changed, just the doc catching up to already-confirmed reality.

**Implementation, diff-based per the locked design (deliberately NOT "animate whatever was clicked"):** reactive skills (Deflect/Ward/Retribution/Vengeance/Intercept) can redirect damage, grant shields, or trigger counter-effects the client never explicitly requested — confirmed real via Phase 3's own `CombatEngine` audit. `combat.gd` now snapshots every combatant's HP/Shield (`_snapshot_combatants()`, keyed the same `Side:Index` way targeting already uses) immediately before every `SubmitAction` call (both Pass and a real card play), then diffs the whole new `CombatStatus` against that snapshot afterward (`_play_hit_feedback`) — covering both teams, not just whoever was clicked or targeted. Shield increase takes priority over an HP change on the same combatant in the same resolution, per the locked design. Feedback is a `create_tween()`-driven portrait modulate flash (red/green/the new silver-blue `HIT_FLASH_SHIELD`) plus a rising, fading floating number — Godot's standard idiom, chosen because this is the first animation-needing feature anywhere in this client (grepped first: no prior `Tween`/`create_tween` usage existed to follow instead).

**A real bug was found and fixed during live verification, not just a client logic bug (second one this project, after Phase 3's "locked object" crash):** the first implementation read a portrait's `global_position` in the same frame `_render_all()` had just freed-and-rebuilt it. Godot's Container layout pass runs during the engine's own process step, not synchronously inside `add_child()` — so a freshly-created Control's `global_position`, queried immediately, is stale (effectively wherever the container's own top-left origin sits, not its real laid-out position). Confirmed live: a "-18" HP-drop number for Marksman rendered near the arena's top-left corner instead of over Marksman's own (rightmost) portrait. Fixed with the exact same "await one frame so layout settles" idiom already used elsewhere in this codebase (`delve.gd`/`hub.gd`/`sanctum.gd`/etc.) — `_play_hit_feedback` now `await get_tree().process_frame`s before reading any portrait's position. Re-verified live after the fix: both a Marksman "-18" and a simultaneous Grovehide "-24" rendered correctly positioned over their own real portraits.

**Real, confirmed edge case (per this task's own step 7 ask) — found, not silently accepted, judged not worth fixing this pass:** a live fight produced exactly the "rises and falls within the same resolution, nets to zero" scenario. Sequence, from the real `EventLog`: Aegis's Fortify granted Marksman +10 Shield (via Inspire's ally-share) in one resolution; a few auto-resolved enemy turns later in that SAME resolution, Grovehide's Claw Swipe fully consumed that 10 Shield (`"18 raw - 10 shield = 8 - 7 def = 1 dealt"`) before Quillfang's Needle Volley then dealt a further plain hit. Net Shield delta across the whole resolution: 0 (10 gained, 10 consumed) — genuinely no Shield animation fires, even though a real Shield-grant event occurred and was later spent. The net HP delta (a real, non-cancelling drop) still correctly triggered the red flash, so the player never saw a false "nothing happened," just a silent miss on the Shield-specific cue for an event that was transient within one resolution anyway. **Judgment: defer, not fix this pass.** A real fix would need either fragile `EventLog` text-parsing or a genuinely new structured per-step-event wire shape (bigger scope than asked here); the practical impact is narrow (a Shield that's granted and fully spent inside one resolution was never actually "held" from the player's perspective by the time the screen updates), and the HP-side of the same event still displays correctly.

**Live-verified against the real running server** (same JWT-bypass technique as prior phases, fully reverted and re-exported clean afterward — confirmed via grep for "TEMPORARY"/"DIAG"/"bypass"/the literal token): a self-heal at full HP (`Guiding Light` on already-100/100 Sprout) correctly produced NO animation, cross-referenced against `EventLog`'s own `"32 healed (0 applied)"` — confirming no false positives. A damage skill blocked by a real position rule (`Snipe`/`Guard Strike` from a position their `Range` doesn't allow) surfaced the server's real rejection rather than a fake success — an incidental, honest confirmation that `GetLegalTargets` doesn't itself check position (only `SubmitAction` does), unrelated to hit-feedback but observed along the way. A Shield-granting self-buff (`Fortify`) produced a visible silver-blue flash + rising number, and its Inspire-shared side effect animated Shield gains on two OTHER, non-targeted combatants in the same resolution (Marksman/Sprout) — confirming the "multiple combatants animate simultaneously" requirement for real. A damage skill (`Shatter`, chosen specifically because its `Range = NA` bypasses the position restriction that blocked several other attempts) produced correctly-positioned, correctly-valued red flashes on two different combatants in one resolution (Marksman "-18" from an auto-resolved enemy counter-attack, Grovehide "-24" from the play itself) after the layout-timing fix.

## Combat Screen — Phase 5 (HandCardSummary.Description, Match Result client — follow-up session)

**Part A — closes TODO #23(b):** `HandCardSummary` gained a `Description` field (`GameHubModels.cs`), populated in `GameHub.cs`'s hand-building code by reusing the exact same `BuildSkillDescription` helper `AnimaPartSummary`/`ReforgeSkillOption` already call — no new formatting logic. `combat.gd`'s hand cards now show this real description (e.g. "Attack · 20 dmg · 2 Energy") instead of the old client-side "<Category> · <EnergyCost> Energy" placeholder, live-confirmed via real server data. Hovering a hand card shows its full description in the docked message area (same `mouse_entered`/`mouse_exited` → set/revert-to-`_render_message_bar()` pattern reforge_node.gd's own hover already established), reverting to whatever the message area was showing before on mouse-leave.

**Real, confirmed bug found and fixed during live verification (third one this project, after Phase 3's "locked object" crash and Phase 4's stale-`global_position` bug):** the hand card's placeholder art block (`art_block`, a plain `ColorRect`) had no explicit `mouse_filter`, so it inherited `Control`'s own default of `MOUSE_FILTER_STOP` — since it's the single largest visual region of the card (48px tall, full width, sitting directly over most of the clickable area), it silently swallowed every click landing on it, before the event could ever reach `card`'s own `gui_input` handler underneath. Confirmed live: clicking the art block never fired `GetLegalTargets`; clicking the skill-name text just below it (`Label`'s own default IS `MOUSE_FILTER_IGNORE`) did. This bug predates this pass — it was present since Phase 1/2/3 built `_build_hand_card`, just never surfaced because prior live-verification passes happened to click text labels, not the art block. Fixed by adding `mouse_filter = Control.MOUSE_FILTER_IGNORE` to `art_block`, and defensively to `name_label`/`effect_label` (already defaulted to IGNORE as Labels, but made explicit) and to the `pip`/`owner_swatch` PanelContainers (same STOP-by-default risk, smaller targets). Re-verified live after the fix: clicking anywhere on a hand card — including the art block — correctly triggers `GetLegalTargets`/targeting.

**Part B — Match Result screens (Victory/Defeat/Retreat), Godot implementation.** Design and backend were already fully locked (see Match Result & Retreat System / Combat System — Server Port sections below) — this was client-only work. Built `match_result.tscn`/`match_result.gd` as a genuinely shared, self-contained component (same child-panel architecture as `augment_picker.tscn` — no Combat-specific or Delve-specific data beyond a plain `params: Dictionary`, no opinion about what happens after the player is done), instanced by **two** different callers:
- `combat.gd`, into a new `MatchResultOverlay` — `_check_for_match_result()` (called after every `SubmitAction`/Pass response) detects the exact response where `CombatStatus.Outcome` first goes terminal and opens the panel in mode `"victory_combat"`/`"victory_elite"`/`"victory_boss"`/`"defeat"`, built from `VictoryReward`/`BossHatchPreview`/`DefeatSummary`.
- `delve.gd`, into the existing `NodeEncounterOverlay` — after a real `RetreatFromDelve()` call, in mode `"retreat"`, fed the returned `DelveEndSummary` directly (same shape Defeat's own summary step reuses, per the locked design's "Retreat... reuses Defeat's layout" line).

Two exit signals distinguish where the caller should end up (not just one shared "done" signal), since a Combat/Elite Victory and everything else have genuinely different destinations: `continue_to_map` (Combat/Elite Victory only — the node is already cleared server-side, so the caller just refreshes and stays on the map) vs. `return_to_hub` (Boss Victory once its Delve Complete summary is dismissed, Defeat, or Retreat — the Delve itself already ended server-side, so the caller navigates to `hub.tscn`). `combat.gd` gained a matching `delve_ended` signal (paired with its existing `resolved`) for delve.gd to distinguish the two cases; `delve.gd`'s `_on_combat_encounter_closed` (the `resolved` handler) now unconditionally calls `_refresh_after_node_resolution()` — safe for both a real Combat/Elite Victory (genuinely changed state) and the pre-existing DEV-only mid-fight escape hatch (no-op refresh).

**Ember sequence** (Combat/Elite/Boss Victory, when `pendingEmberColors` is non-empty): resolves one color at a time via the exact same shared `augment_picker.tscn` Shop's own Ember purchase already uses (`(hub, ember_color)` signature, zero changes needed), advancing a local index on each `resolved` — no extra RPC needed to know "is there another one queued," since `ConvertPendingEmberToWisp`/`AugmentPendingEmber` always resolve the front of the real server-side queue first (confirmed via `GameHub.cs`), so replaying the captured front-to-back list locally stays correct.

**Boss Victory's full ceremony:** team HP strip (same green/amber/red color-coding convention as combat.gd's own arena, duplicated per this codebase's established per-file small-mapping pattern) → Wisp/Echo Shard/Ember sequence → reveal step reusing `AnimaRevealPanel` (the exact same component the Starter Trio flow already refactored into a reusable shape), fed `bossHatchPreview` with `gen=1`/`default_name=""` (no archetype to suggest, matching a Weave/Boss-hatch offspring's own "" convention) → on `confirmed(name)`, calls `ConfirmBossHatch` directly → complete step shows the named Anima + the `DelveComplete` summary (floors reached, nodes cleared, team, total Wisp) → `return_to_hub`.

**Retreat button visibility (delve.gd):** gated to "standing on the map between nodes" per the locked design — hidden by default in `delve.tscn`, shown once `_run()`'s `StartDelve` succeeds, hidden again on `_open_node_encounter`/`_open_combat_encounter` (entering any node), re-shown on `_on_node_encounter_resolved`/`_on_combat_encounter_closed` (back on the map) or hidden permanently once a scene-navigating outcome fires. **Pre-existing scaffolding found and reused, not rebuilt:** the Retreat button, its confirm modal (`RetreatOverlay`), and the `RetreatFromDelve()` wiring already existed in `delve.gd`/`delve.tscn` as uncommitted work from an earlier pass at this same task, with a stub inline summary explicitly commented "Match Result screens don't exist yet either... stands in for the fully designed... result screen." This pass replaced that stub (`_show_end_summary`, plus the now-dead `EndSummaryPanel` scene nodes it drove) with the real shared `match_result.tscn` component — the confirm-modal half was correct as found and needed no changes.

**Verification methodology:** headless scene-instantiation checks (`match_result.tscn`/`combat.tscn`/`delve.tscn` all load cleanly, zero script errors — confirmed via a throwaway `SceneTree`-script pass) plus real live verification against the real running server (JWT-bypass technique, two fresh accounts, fully reverted and re-exported clean afterward — confirmed via grep for "TEMPORARY"/"DIAG"/"bypass"/the literal tokens). **Real, live-verified:** the art-block click-bug fix itself (confirmed via the `GetLegalTargets` send/no-send difference in the browser console before/after); the full targeting → highlight → Confirm → `SubmitAction` → hit-feedback loop across many real Rounds (Phase 3/4 features re-confirmed intact after this pass's changes, including a real `Stunned`/`Bleed`/`Thorns`/`Frenzy` status mix); hover-on-click-through showing a card's real description in the message area; and — full end-to-end — **Retreat**: real `RetreatFromDelve()` call → `match_result.tscn` in `"retreat"` mode rendering the exact locked copy ("Delve Retreated", "Wisp earned this run: 0 -> kept: 0", "Floor reached: 1 | Nodes cleared: 0", the Shards-kept/Artifacts-lost note) → `return_to_hub` → real navigation back to `hub.tscn`.

**Honestly flagged, not silently skipped: Combat/Elite Victory's compact-tier match_result screen and Boss Victory's full ceremony were NOT reached via a real live fight-to-completion this pass.** A real Floor-1 fight (Marksman/Aegis-equivalent Crimson/Onyx/Verdant starter trio) was fought turn-by-turn toward Victory and got Grovehide (60 HP) to 0 (confirmed dead, removed from turn order) before time/effort constraints made continuing to grind down Quillfang (40 HP, Verdant's 0.7x damage multiplier making this exceptionally slow — many rounds passed with zero eligible attack cards in hand at all, since Warden/Boulder-equivalent had no Attack-category skills whatsoever) impractical to finish manually within this pass. This is a genuine gap, not a silently-assumed pass: the Victory-mode branch of `match_result.gd` (reward row, Ember sequence, Continue button, and for Boss the HP strip/AnimaRevealPanel/Delve-Complete chain) is code-reviewed and structurally identical to the live-verified Retreat/summary branch (same component, same render pipeline, only the `mode`/`params` differ), and `SubmitAction`'s reward-granting path was exercised indirectly via the same `CombatStatus` responses throughout the fight — but the terminal Victory screen's own rendering was not visually confirmed live. Boss Victory's Anima Reveal chain additionally rests on the same reasoning this task's own instructions explicitly allowed ("verified via the existing `PendingBossHatch` test pattern if reaching a live Boss fight isn't practical") — `ConfirmBossHatch`/`GetPendingBossHatch`'s real signatures were already confirmed by reading `GameHub.cs` directly (not assumed), and `AnimaRevealPanel`'s own reuse here is identical to its already-live-verified use in the Starter Trio flow.

## Match Result & Retreat System (NEW THIS SESSION — fully designed AND implemented)

**Victory Screen — two tiers, one shared component, gated by `isBoss`:**
- **Combat/Elite (compact, fast to proceed):** small header ("Enemy Defeated"/"Elite Defeated"), Wisp reward, Ember pickup sequence (only if ≥1 dropped, reuses existing EmberService/AugmentService flow as-is), Vessel Shard pop-in (Elite only, 25%), Continue → back to Delve map, node cleared. **No team HP strip on this tier** — HP is already visible on the map screen itself.
- **Boss (full ceremony):** big header ("Warden of the Hollow Defeated"), team status strip (post-combat HP, same color coding as Combat screen), Wisp reward, Ember sequence if any, guaranteed Echo Shard, **guaranteed hatched Anima via the Anima Reveal screen** (see below). Continue → **Delve Complete** state: same component + appended summary block (floors reached, Anima used, total Wisp earned) → Return to Hub.

**Defeat/Wipe Screen:** header "Delve Ended" (not "You Died" — cozy-but-dark tone, not punishing). **Wisp penalty (LOCKED): keep 50% of Wisp earned *this run only*** — Wisp already banked from prior runs is untouched. Echo/Vessel Shards always kept in full, no penalty ever. Artifacts (run-only) are lost. Summary shows the math explicitly ("Wisp earned this run: 340 → kept: 170"), plus floor/node reached and nodes cleared. Single "Return to Hub" button, no retry-from-here (matches "no partial-death/revival").

**Retreat (mid-run voluntary exit, NEW):** button on the Delve map screen, visible whenever standing on the map between nodes (NOT available mid-combat/inside a node). Confirm modal (misclick protection, same pattern as Combat's Confirm/Cancel): "Retreat from this Delve? You'll keep your Wisp and Shards, but lose any held Artifacts. Retreat / Cancel." **Penalty (LOCKED): 0% Wisp penalty — keep 100% of this-run Wisp.** Artifacts still lost (run-only). This is deliberately better than a wipe (50% keep) so Retreat reads as a genuine "cash out" decision, not a soft-fail. Result screen reuses Defeat's layout, header changed to "Delve Retreated."

**Backend implemented:** `RunLedger.WispAtDelveStart` (snapshot at Delve start, so end-of-run math never touches pre-run banked Wisp) + `DelveEndService.ResolveDefeat`/`ResolveRetreat` (50%-keep and 100%-keep resolution), both returning a shared `DelveEndResult` shape for the one result-screen component.

## Boss Reward & Anima Reveal Screen (NEW THIS SESSION — fully designed AND implemented)

**Boss reward, final structure:** guaranteed Wisp (300) + guaranteed hatched Anima (resolves via Anima Reveal screen, not a banked resource — the old `ResourceType.Vessel` concept is fully retired, removed from the codebase entirely) + guaranteed Echo Shard.

**Boss-hatch Anima roll logic (no lineage/parents involved), implemented in `BossHatchService.Roll`:**
1. Roll body color, flat 25% each, Crimson/Onyx/Verdant/Azure. **No hybrids** — Vulcan/Mirage excluded entirely.
2. Roll each of the 4 parts' color independently, weighted **55% toward the Step 1 body color, 15% each for the other 3.**
3. Roll each part's Dominant, R1, and R2 as three independent 55/15/15/15-weighted rolls (via `SkillPool.RollRandom(Part, AnimaColor, Random)`), then pick the actual skill from that rolled color's pool. This makes Boss-hatch genomes **structurally identical to Weave-produced ones** — `IsFullyPure`, `WeavingService`, and materialization all work with zero special-casing (tested: `IsFullyPure` lands at the expected 0.55⁴ ≈ 9.15%).
4. Gen is fixed at **Gen 1** (wild origin point, same tier as starter trio conceptually, but goes through the normal mandatory naming prompt — starter trio remains the sole hardcoded-name exception).

**Anima Reveal Screen — shared component, two triggers:** (a) Boss Victory, when the guaranteed Anima resolves, (b) Weaving screen, on Weave completion. Layout: portrait reveal → Name/Color/Gen line → Threads section (Dominant/R1/R2 per part with the same "Show hidden" toggle default as Profile — **uniform display for both triggers, no Boss-hatch special case**, since Boss-hatch genomes are now structurally complete) → mandatory naming prompt (reuses Weave-creation naming pattern) → Confirm/Continue. Confirm commits the name, adds the Anima to the Sanctum roster, and returns to the triggering flow (Boss Victory → Delve Complete; Weaving → normal screen state).

**Backend implemented:**
- `Models.Anima` gains required `Name` (mutable, distinct from Id — `DisplayName` now returns `Name`) and `Gen` (int) fields. All 13 `SampleAnimas` factories updated (Name = Id, Gen = 1).
- `AnimaMaterializationService` (new, shared by both triggers): Weave overload computes `Gen = max(parentA.Gen, parentB.Gen) + 1` and records lineage; Boss-hatch overload fixes `Gen = 1`, no parents, looks up Stats via `WeavingService.ColorStats`. Both mint a fresh Guid Id and add the result to `SanctumRoster`.
- `SanctumRoster` (new) — the actual owned-Anima storage; this was a real confirmed gap, nothing tracked owned Animas anywhere before this session. Minimal `List<Anima>` + `FindById`, in-memory only (same no-save/load caveat as `PersistentLedger`).

**Future economy note:** Boss's guaranteed-Anima drop is currently a placeholder economy sink for the pre-trading era. If a future trading/marketplace system (Axie/Pokémon-style player-to-player exchange) makes Animas abundant, revisit disabling or reducing the guaranteed Boss drop so Weaving/trading remain the primary Anima sources rather than Boss-farming. See "Open Design Threads" in Anima_Design_Doc.md for the full list of deferred/open items.

## DelveRun — Run/Traversal State Model (NEW THIS SESSION — implemented)

`Anima.Core.Run.DelveRun` is the missing glue between node visits — the "save file for a single in-progress run." Scope was deliberately kept minimal (in-memory only, no save/resume, no mid-combat state):

- **`CurrentNode`/`AvailableNodes`/`TryMoveTo`** — map position on the 7×15+Boss grid. Boss surfaces automatically as an available node with no special-casing needed.
- **`ClearedNodes`** — caller-driven (not automatic on move), so the map can render visited vs. available.
- **`Team`** — a plain `List<Anima>`, the same list a Combat node's `CombatState.PlayerTeam` is built from. HP attrition needed zero separate tracking as a result — `CurrentHp` mutations persist for free through the shared reference, **as long as nothing ever rebuilds/clones this list** (see gotcha above).
- **`CurrentArtifacts`/`WispEarnedSoFar`** — live passthroughs to `RunLedger.Artifacts`/`PersistentLedger`, not separate copies, to avoid forking state that has to be hand-synced.
- **`DelveRun.Start(...)`** — the one real call site that sets `RunLedger.WispAtDelveStart` from the current `PersistentLedger` balance. **Bugfix note:** `WispAtDelveStart` existed since the Match Result & Retreat session but nothing outside test code ever actually set it — every real Delve would have diffed Wisp-earned against a stale 0 before this fix.

**Explicitly out of scope for this pass (by design, not oversight):** no mid-combat state (an active Combat node owns its own state; `DelveRun` just receives the result on resolution), no save/resume across app restarts (matches `SanctumRoster`/`PersistentLedger`'s existing no-save/load caveat), no multiplayer/concurrency handling (single active `DelveRun` at a time).

## Combat System — Server Port (Phases 5a–5d, LOCKED)

Core Combat loop, rewards, Boss-hatch, Delve-Complete summary, and Anima Profile Delve History are all real and ported onto `GameHub`.
- **Core loop (5a):** `StartCombat`/`SubmitAction`/`GetLegalTargets`/`GetCombatState`. Resumable engine — `CombatEngine.AdvanceUntilPlayerActionNeeded()` auto-resolves enemy turns/Round transitions until a player action is needed or the fight ends; `ResolvePlayerAction`/`ResolvePlayerPass` validate turn/ownership/usability/Energy server-side. `GetLegalTargets` computes the real legal-target set per skill for the client's highlight-then-confirm flow. `ActiveCombat` is in-memory only (same lifetime as `ActiveDelveRun`).
- **Rewards (5b):** `SubmitAction` resolves Victory (Combat/Elite/Boss)/Defeat rewards inline the moment a winner is determined — Wisp/Ember/Vessel Shard for Combat/Elite, Wisp+Echo Shard+guaranteed Boss-hatch (via `PendingBossHatch`, DB-backed, mirrors `PendingWeave`) for Boss, 50%-Wisp-keep math for Defeat. `RetreatFromDelve` (100% Wisp keep) is rejected mid-combat.
- **Delve Complete summary (5c):** floors reached/nodes cleared/team/total Wisp earned, captured at Boss Victory and carried on `PendingBossHatch` until `ConfirmBossHatch` returns it alongside the named Anima.
- **Anima Profile Delve History (5d):** lifetime `CompletedDelveCount`/`FailedDelveCount` on `Models.Anima` (Retreat counts as Failed) + a capped last-5 detailed log (`DelveHistoryEntity`: outcome, floor, combats/elites/boss cleared, team, Wisp earned), surfaced via `GetAnimaDetail`.

(full history: docs/history.md)

## Server / Accounts / Auth (LOCKED — committed)

`src/Anima.Server` is a real ASP.NET Core project (Auth/JWT/SignalR `GameHub`/DB persistence), built across several sessions, all committed. Auth: register/login/password-reset (PBKDF2 via `PasswordHasher<T>`, JWT 180-day bearer tokens, no full ASP.NET Identity). Persistence: EF Core on Postgres (`AnimaDbContext`), per-account `SanctumRosterRepository`/`PersistentLedgerRepository`, `AccountLockRegistry` serializes writes per account. SignalR: `GameHub`, JWT via `access_token` query string (needed for transports that can't set headers), per-connection `PlayerSession` loads roster/ledger/team/pending state on connect. Local dev: Postgres via `deployment/docker-compose.yaml` (gitignored `.env` for credentials), connection string/JWT key via `dotnet user-secrets` (see `src/Anima.Server/README-dev-secrets.md`) — no secrets checked in. `GameHub` now covers the full game loop — Sanctum/Collection/Anima Profile/Weaving/Delve traversal/Resource/Treasure/Shop/Combat (loop+rewards)/Boss-hatch/Match Result/Delve Complete/Delve History/Reforge/Starter Trio. **Known gaps:** no test coverage (`tests/Anima.Server.Tests` is still the unmodified template); no real email delivery for password reset (`LogEmailSender` stand-in only). (full history: docs/history.md)

## Godot Client Connectivity (LOCKED — plumbing milestone)

Real `client/` Godot 4.7.1 project (GDScript, Standard/non-.NET — Godot 4.x C# can't export to Web) proves the `GameHub` connectivity pipe end to end through a real exported Web build in a real browser. Uses the `GDSignalR` addon (vendored, MIT, `HenryAbraham733/godot-signalr`) — connects directly via `WebSocketPeer`/JSON Hub Protocol, skipping `/negotiate`; auth via `?access_token=<jwt>` query string. CORS policy (`DevClientCorsPolicy`) allows the local static-file-server origin for the two REST Auth endpoints (not needed for the WebSocket hub itself). Standing smoke-test scene: `connectivity_test.tscn`/`.gd` (kept, not deleted, as a regression check). **Known gaps:** no transport fallback (WebSocket only); a dropped connection reconnects into a fresh `PlayerSession` (in-memory `ActiveDelveRun`/`ActiveCombat` are lost — no resume); JWT is sent in the query string (standard SignalR pattern, but logs/history exposure caveat applies). (full history: docs/history.md)

## Login / Create Account Screen (LOCKED — first real UI screen)

`login.tscn`/`login.gd`, tab toggle between Login/Create Account on one scene, warm sanctuary/workshop theme. `ApiClient` (shared static REST helper) + `AuthState` (autoload, in-memory JWT/session only — no persistent storage yet). Contract: `POST /api/auth/login` `{Username, Password}`, `/register` `{Username, Email, Password}`, both return `{AccountId, Username, Token, ExpiresAtUtc}`; 401/409 errors surfaced in-UI. On success, hands off to `hub.tscn` (renamed from `post_login.tscn`), which reconnects the hub and calls `GetRoster()` to confirm a live session. `project.godot`'s `run/main_scene` points at `login.tscn`; `AuthState` is autoloaded. (full history: docs/history.md)

## Known TODOs
1. ~~No Run/traversal-state model~~ — resolved, `DelveRun` implemented.
2. ~~Combat/Elite/Boss room background palettes~~ — resolved, all 3 implemented.
3. ~~Godot client: not started~~ — stale; real client now exists (Login, Hub, Starter Reveal, placeholders) with `Anima.Server` fully backing it. See Godot Client Connectivity / Hub Screen / Server sections.
4. Cross-color hybrid PvE combat value: correctly deprioritized (PvP-relevant, out of scope).
5. No partial-death/revival concept — a wipe just ends the Delve. Wisp reward amounts are first-pass, need tuning.
6. Boss's guaranteed-Anima drop is a placeholder economy sink — revisit if a future trading/marketplace system makes Animas abundant (see Boss Reward & Anima Reveal Screen above).
7. ~~Shop room Augment section~~ — resolved, removed entirely; Augmenting only happens via Ember pickup/purchase.
8. ~~Energy cap discrepancy~~ — resolved, +3/Round capped at 9 confirmed correct.
9. ~~Design the match result screen~~ — resolved, Victory/Defeat/Retreat/Anima Reveal all designed and implemented.
10. ~~`ReforgeService.Accept` Crest handling~~ — resolved, Crest scoped out of Reforge entirely; Wisp tiers (40/80) also resolved.
11. ~~Guaranteed-minimum-1-Elite-per-Delve + Floor 1-5 Elite exclusion~~ — resolved, implemented in `MapGenerator`, re-validated at 500 seeds.
12. ~~Anima Profile Delve History tracked nowhere~~ — resolved (Phase 5d), see Combat System section above.
13. ~~Reforge map odds/flow-order/GameHub wiring/visual identity~~ — resolved, see Reforge section above.
14. ~~Hardcoded Ember/Boulder/Sprout starter trio~~ — resolved, see Starter Trio section above.
15. ~~Real Hub screen, real Aspect/enemy art~~ — resolved, see Hub Screen section above.
16. ~~Enemy sprites copied into `client/assets/enemies/` but NOT wired up yet~~ — resolved, Combat Screen Phase 1 wires all 5 real enemy sprites via `ENEMY_SPRITES` in `combat.gd` (see Combat Screen — Phase 1 Implementation above).
17. ~~Hub layout bugs (flat-bar cards, invisible portraits)~~ — resolved, root cause was missing `custom_minimum_size`; see Hub Screen section above.
18. **Still unresolved, and confirmed WORSE this session (Reforge screen pass): Godot Web-exported `HTTPRequest` fails (reported "Could not reach the server") on `/api/auth/login`/`register`** — SignalR/GameHub calls unaffected. CORS, IPv4/IPv6 mismatch, cold-start, and chunked-transfer/`Content-Length` framing have all been tested and ruled out in prior sessions (see docs/history.md). **This session's escalation:** the failure reproduced 100% of the time (~35 real login/register attempts across 6 separate Web exports, 0 successes — a materially worse rate than prior sessions' "usually succeeds within a handful of retries"), and for the first time a real root-cause SIGNAL was captured, not just ruled-out suspects: temporary debug prints traced the failure to `ApiClient.post_json`'s `result` value, which was either `HTTPRequest connection-level result 13` (Godot's `HTTPRequest.RESULT_TIMEOUT` — i.e. a genuine ~10-20s connection timeout, not an instant rejection) on some attempts, or an instant failure on others — while a raw `fetch()` call to the identical endpoint from the same page's JS context (via a browser console eval) still succeeded immediately every time, confirming the divergence is specifically inside Godot's own `HTTPRequest`/Emscripten networking bridge, not the server, CORS, or response framing. Multiple environmental variables were ruled out this session too: number of open browser tabs (reproduced identically with 6 tabs and with exactly 1), fresh vs. reused tabs, and browser HTTP cache (hard-reloaded/cache-busted). Root cause is still unknown, but now narrowed to "Godot Web's `HTTPRequest` polling/completion path, not the underlying browser network stack." See Reforge section's Godot implementation note below for how this blocked live click-through verification of the new Reforge screen. **Follow-up session (Delve `_team_list` fix): the "SignalR/GameHub calls unaffected" clause above may no longer hold** — a real Hub connection succeeded once via a test-only auth bypass, but the identical technique then hung indefinitely on "Connecting to GameHub…" for a second account, and re-testing the FIRST (previously-working) account afterward hung too. A raw Node.js WebSocket test confirmed the server answers correctly and instantly, isolating the hang to Godot's client-side networking bridge again, this time on the WebSocket path rather than `HTTPRequest`. Not investigated further this pass (out of scope) — flagged here as new information for whoever picks up this TODO next.
19. **Minor, not blocking: pressing Enter in `login.gd`'s username/password fields double-fires the submit handler** — two near-simultaneous identical POSTs per Enter keypress. Confirmed harmless and confirmed not the cause of item 18. Worth a real fix before this screen ships, but not urgent.
20. ~~Shop/Reforge/Combat node screens still placeholder in Godot~~ — Reforge resolved a prior session (`reforge_node.tscn`/`.gd`). Shop resolved a prior session (`shop_node.tscn`/`.gd`, Rest + Wares only). Combat Phase 1 (static layout + real-data rendering, no player interaction) resolved this session (`combat.tscn`/`.gd` — see Combat Screen — Phase 1 Implementation above); the real interactive loop (card play/targeting, SubmitAction, win/loss) is Phase 2+, not yet built.
21. ~~Shop's Ember purchase was stubbed, not real~~ — resolved this session. Real, shared `augment_picker.tscn`/`.gd` built (Augment now / Convert to Wisp), instanced as a child overlay by `shop_node.gd` on a successful `BuyWaresEmber`, same child-panel/shared-connection pattern the other node screens use. Takes only `(hub, ember_color)` — no Shop-specific data — so it's directly reusable by a future Combat Ember-drop with zero changes. **Real RPC surface confirmed by reading GameHub.cs, not CLAUDE.md prose (this session's Shop findings already showed the prose had drifted once):** `ConvertPendingEmberToWisp()` (flat +15 Wisp, confirmed unchanged, deliberately NOT Ember-Core-discounted) / `AugmentPendingEmber(AugmentPendingEmberRequest{AnimaId, Part, AugmentType})` (identifies the target skill by Anima+Part, not a raw skill id) — both return the pending-Ember-colors list on success, null on rejection, no distinct structured outcome (unlike `AcceptReforge`). There is no standalone "peek the pending Ember" RPC — `GetPendingEmberColorsAsync` is a private GameHub helper only ever returned as a side-channel field on other RPCs' results — so the picker takes the Ember's color as a caller-supplied parameter instead. **Real, confirmed gap closed as part of this work:** `AnimaPartSummary` (GetRoster's per-part shape) had no per-part `Color` field before this session, despite the locked eligibility rule being "the skill's own archetype color, not the Anima's body color" (what makes hybrid Vulcan/Mirage Animas' individually-colored parts work for free) — added `AnimaPartSummary.Color` (nullable string, mirrors `Skill.Color?.ToString()`) server-side (`GameHubModels.cs`/`GameHub.cs`), verified live end-to-end (a Verdant Ember's browse step correctly showed only the Verdant team member's 4 parts, excluding a Crimson and an Onyx teammate entirely). `AugmentService.GetNextAugmentCost`/`IsApplicable` are Core-only, never exposed via GameHub — the picker re-derives the next-tier Wisp cost client-side from the already-wire-exposed `AppliedAugments.Count` against the locked, stable `[15,30,50]` curve (shown as an estimate, Ember Core's discount not reflected until a real confirm), and does NOT try to replicate per-type applicability (AoE Damage only makes sense on certain skills, etc.) since the wire data to do that correctly isn't exposed either — an inapplicable pick just surfaces the server's real rejection at confirm time instead of being silently pre-filtered. **Known minor gap, flagged not fixed:** one live InsufficientWisp rejection this session came back with a generic client-side fallback message instead of the real captured server text, for a cause not yet root-caused (see `augment_picker.gd`'s own `_invoke_capturing_error` comment) — not a blocker, since the client-side affordability check already prevents this case in normal play, but worth a look before Combat's own Ember-drop wiring leans on the same helper.

22. ~~Every real materialized Anima's `Position` defaults to a hardcoded `1`, no distinct front/mid/back positions assigned before a fight~~ — resolved, Combat Screen Phase 2 fixed this in `GameHub.StartCombat()` (fresh 1/2/3 assignment every genuine new fight, by team list order). See Combat Screen — Phase 2 above.
23. **Combat Screen: two real wire-shape gaps — (a) still open, (b) resolved this session (Phase 5)** — (a) `CombatantSummary` still has no `AnimaId` field — the client still name-matches Player-side combatants against `GetRoster()` for arena sprite selection (unchanged, still not a strict identity guarantee; Phase 3's audit confirmed this gap is irrelevant to targeting, which uses `Side`/`Index` instead); (b) ~~`HandCardSummary` still has no description/effect-text field~~ — resolved, Combat Screen Phase 5 added `HandCardSummary.Description` (server-synthesized via the same `BuildSkillDescription` helper `AnimaPartSummary`/`ReforgeSkillOption` already use) and wired it into both the hand card's caption and its hover text. See Combat Screen — Phase 1/Phase 2/Phase 3/Phase 5 sections above.
24. ~~Combat Screen Phase 2: real targeting/play (`GetLegalTargets`, Confirm/Cancel, `SubmitAction` with a real `HandIndex`/`Target`) still entirely unbuilt~~ — resolved, Combat Screen Phase 3 (see above): full `GetLegalTargets` → highlight/skip → Confirm/Cancel → real `SubmitAction` flow implemented and live-verified for both a self-targeted and a non-self-targeted skill across 2 Rounds. A real "freeing a locked object mid-signal-emission" crash was found and fixed along the way (see Phase 3's own writeup).
25. **Combat Screen Phase 2's unexplained "double render" observation — still not root-caused, not seen again this pass (Phase 3), but not deliberately re-investigated either** — a temporary diagnostic print previously showed each real state change logged twice in immediate succession with identical (always correct) data. Not chased further — flagged for whoever next debugs something that smells like a duplicate signal fire or duplicate RPC call in this screen.
26. ~~Combat Screen Phase 3: hit-feedback animations (floating +/- numbers, portrait flash) still unbuilt~~ — resolved, Combat Screen Phase 4 (see above): diff-based floating numbers + portrait flash for HP/Shield changes, live-verified including multi-combatant simultaneous animation and a real confirmed edge case (a Shield rise-then-fall netting to zero within one resolution shows no Shield flash — see Phase 4's own writeup).
27. **LOW PRIORITY, not yet investigated: a real, unrelated `hub.gd:307` error, observed multiple times across Phase 3 and Phase 4's live-verification sessions but never chased** — `ERROR: Parameter "data.tree" is null` / `Invalid access to property or key 'process_frame' on a base object of type 'null instance'` inside `_connect_hub`'s `await get_tree().process_frame`, on some (not all) fresh page reloads. Never once blocked anything (the connection always succeeded moments later regardless) and is unrelated to any Combat-screen change in either phase — flagged for whoever next touches Hub's connection-retry path, explicitly not urgent.
28. ~~Combat Screen: hand card's placeholder art block (`ColorRect`) had no `mouse_filter`, silently swallowing clicks landing on it (Control's own `MOUSE_FILTER_STOP` default) before they could reach the card's `gui_input` handler~~ — resolved, Combat Screen Phase 5 (found during live click-through: clicking the art block never fired `GetLegalTargets`, clicking the text below it did). Fixed by setting `MOUSE_FILTER_IGNORE` on `art_block`/`name_label`/`effect_label`/`pip`/`owner_swatch`. Present since Phase 1/2/3 built `_build_hand_card`, only surfaced now because this pass happened to click the art block during verification.
29. **Combat Screen Phase 5: Match Result's Victory-mode screens (Combat/Elite compact tier, Boss full ceremony) were not reached via a real live fight-to-completion — code-reviewed and structurally verified only, not visually confirmed live.** A real Floor-1 fight was fought to Grovehide's death but Quillfang (Verdant's 0.7x damage multiplier, very slow) wasn't finished off within this pass's time budget; Retreat's own result screen (same shared `match_result.tscn` component, same render pipeline, different `mode`) WAS fully live-verified end-to-end. Whoever next has a reason to touch `match_result.gd` should try to reach a real Victory (Elite/Boss too) and confirm the reward row/Ember sequence/Continue button (and for Boss, the HP strip/AnimaRevealPanel/Delve-Complete chain) render as designed.

## Working Preferences
- User is a C# backend dev, no frontend experience — Claude does all coding, user handles deployment.
- Trusts Claude's design/implementation calls once discussed — flesh out design in chat first, then give a ready-to-send CC prompt (no need to ask "want me to send this").
- Highly values honest reporting of bugs/gaps/inconclusive results, and honest correction when Claude makes a mistake (e.g. a wrong icon choice, or a discrepancy between an assumption and actual implemented behavior).
- Calls Claude Code "CC" in the separate design conversation where all game design decisions get made.
- Values token/effort economy — willing to skip mocking up every variant (e.g. Elite/Boss palettes) when a verbal description is sufficient to confirm direction.
